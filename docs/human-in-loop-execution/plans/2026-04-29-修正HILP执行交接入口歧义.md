# 执行计划：修正 HILP 执行交接入口歧义

HILP design asset_ref: `stage-3/design-choice@v1 [state=approved｜中文状态=已批准]`
HILP blueprint asset_ref: `stage-4-5/implementation-blueprint@v2 [state=approved｜中文状态=已批准]`
HILP execution handoff asset_ref: `stage-6/execution-handoff@v1 [state=archived｜中文状态=已归档]`
禁止越界项:
- 不修改 `human-in-loop-planning/**` 源文件。
- 不修改 `human-in-loop-execution/references/execution-routing.md`。
- 不修改 `human-in-loop-execution/references/writing-plans.md`。
- 不修改除本交接列明的 2 个 execution skill 文件之外的 skill 源文件。
- 不修改其他 `docs/hilp/**` 历史变更目录。
- 不修改 `docs/review/**`。
- 不改变已批准设计和蓝图的语义。
目标: 修正 execution skill 对执行交接资产是否必须 approved 的歧义，并验证当前变更目录 Markdown 表格列数一致。
执行约束: 单代理顺序执行；只修改 `human-in-loop-execution/SKILL.md` 与 `human-in-loop-execution/references/hilp-handoff-intake.md` 两个目标源文件；纯文档规则变更使用执行交接列出的替代验证命令。

## 文件结构和文件职责

| 文件 | 职责 | 允许改动 |
|---|---|---|
| `human-in-loop-execution/SKILL.md` | execution skill 顶层触发、入口前提、路由纪律与输出纪律。 | 修改 description、入口前提、缺失回退说明、HILP 绑定纪律中的已批准输入表述。 |
| `human-in-loop-execution/references/hilp-handoff-intake.md` | 执行交接接收时的输入契约、有效性检查和禁止事项。 | 修改输入契约、执行规则第 3/6 条、禁止事项、检查清单第三项。 |
| `docs/hilp/修正HILP执行交接入口歧义/**` | 本轮规划与交接资产。 | 只读取和验证表格列数；执行阶段不再修改。 |

## 任务清单

### 任务 1：验证当前变更目录表格列数

- 文件路径：`docs/hilp/修正HILP执行交接入口歧义/**.md`
- 目标：确认已修复的 review-pack 和新增资产均满足表头、分隔行、数据行列数一致。
- 验证命令：运行蓝图中的 Node 表格列数脚本。
- 预期输出：退出码 0，输出 `markdown table columns ok`。
- 最小实现：不修改文件，只运行验证。
- 回归验证：同一命令在最终完成前再次运行。
- 变更记录：无源文件变更。

### 任务 2：修改顶层 execution skill 入口语义

- 文件路径：`human-in-loop-execution/SKILL.md`
- 目标：把“execution handoff approved”修正为“handoff intake completed with no blocking items”，并明确执行交接资产自身不要求已批准。
- 验证命令：
  - `! grep -R "handoff has been approved" -n human-in-loop-execution/SKILL.md`
  - `grep -R "执行交接资产自身不要求" -n human-in-loop-execution/SKILL.md`
  - `grep -R "owner_skill=hilp-execution-handoff" -n human-in-loop-execution/SKILL.md`
- 预期输出：第一条退出码 0 且无匹配；第二、三条均输出 `human-in-loop-execution/SKILL.md` 匹配行。
- 最小实现：按蓝图替换 description、入口前提、缺失回退说明和 HILP 绑定纪律一条 bullet。
- 回归验证：最终完整验证命令集合。
- 变更记录：记录在最终完成摘要中。

### 任务 3：修改执行交接接收规则

- 文件路径：`human-in-loop-execution/references/hilp-handoff-intake.md`
- 目标：把执行交接 asset_ref、owner、落盘、无阻断、范围和停止条件定义为有效性检查，排除“自身必须 approved”的误读。
- 验证命令：
  - `grep -R "执行交接资产自身不要求" -n human-in-loop-execution/references/hilp-handoff-intake.md`
  - `grep -R "owner_skill=hilp-execution-handoff" -n human-in-loop-execution/references/hilp-handoff-intake.md`
- 预期输出：两条命令均输出 `human-in-loop-execution/references/hilp-handoff-intake.md` 匹配行。
- 最小实现：按蓝图修改输入契约代码块、执行规则第 3/6 条、禁止事项新增一条、检查清单第三项。
- 回归验证：最终完整验证命令集合。
- 变更记录：记录在最终完成摘要中。

### 任务 4：运行完整验证并检查越界

- 文件路径：仓库工作区。
- 目标：证明执行结果满足交接验证承诺且未修改 planning 源文件。
- 验证命令：
  - `! grep -R "handoff has been approved" -n human-in-loop-execution/SKILL.md`
  - `grep -R "执行交接资产自身不要求" -n human-in-loop-execution/SKILL.md human-in-loop-execution/references/hilp-handoff-intake.md`
  - `grep -R "owner_skill=hilp-execution-handoff" -n human-in-loop-execution/SKILL.md human-in-loop-execution/references/hilp-handoff-intake.md`
  - `git diff -- human-in-loop-planning`
  - 蓝图中的 Node 表格列数脚本
- 预期输出：前四项符合蓝图；Node 脚本输出 `markdown table columns ok`。
- 最小实现：只运行命令，不再修改文件。
- 回归验证：同任务验证输出作为完成前证据。
- 变更记录：把命令、退出码和摘要写入最终回复。

## 自检

- 蓝图覆盖：已覆盖 `hilp-asset-table-rendering`、`skill-entry-description`、`handoff-intake-contract` 三个切片。
- 占位符扫描：本计划没有 TODO、TBD、后续再定、类似上一步、写适当测试、补齐错误处理、按需实现。
- 类型 / 方法签名一致性：不涉及代码类型或方法签名。
- 禁止越界项检查：计划只触及两个目标源文件和验证读取当前 HILP 资产。
