# 状态: 已关闭
# 类型: task
# 阻塞于: 无 (M03/M04 均已关闭)

## 关闭摘要 (2026-09-15)

8 ISSUE 全部收口, 82 项契约测试全绿 ([EXECUTION](../milestone-05/EXECUTION.md), 13 条 UD [账本](../milestone-05/UNAUTHORIZED_DECISIONS.md)): birth 发布 8800 + STATE web-port; `--lan-ip` 已确认地址机制 (DECIDE kind lan-address 入收据闭环); birth/resume/status 双 URL 交付 (只列自身, 未确认地址打未附发提示, status 只对 running 附行); 一母体一活跃容器 (活跃=非 retired); present `--fixed-port` 锁端口 + 容器分支钉 8800; SKILL.md 展示沟通指引 (当前设备/宿主定位/降级) 与新规则对齐 + 分发与生效节. 待验收 (不记通过): TC-001/TC-004 真机双机归 M09; present 规则入容器需 host 侧 sync-to-pi + base 重建 (UD-08, 与 M08/M09 协调).

## 问题

网页访问实施. 权威输入: [M04 账本](../milestone-04/DECISIONS.md) D001-D013, ADR [0012](../../../adr/0012-container-web-direct-access.md). 只处理改动后新建的容器, 不改造/重建现有容器.

## 实施范围

1. swt.py birth 补 `-p 8800`, 宿主 0.0.0.0 动态端口; STATE 登记 web-port, 对新容器的 birth/status/resume 保持映射和交付一致. 每次只给当前母体对应容器的本机/局域网双 URL.
2. 新 birth 遵守一母体一个活跃容器, 修正原先允许同母体多活跃容器的规则. 不追溯处置既有容器; 同主仓单活动母体限制仍成立.
3. use-sandbox-worktree skill 与 present 容器分支对齐 `0.0.0.0:8800`, 多张页面复用现有 present 服务. 不假设存在 Vite/Next.js 冲突, 不引入切换停服务/反向代理.
4. 工作开始确定当前设备, 能从会话确定就自动记录, 不确定才问一次, 切换时更新; 展示信件显式指定该设备. 容器经信箱询问设备 agent 取得正确网址, 经既有 ssh/herdr 回告原会话, 页面就绪后在该设备打开一次. 指引要带齐容器/原会话定位信息并验证接收方实际能查到网址, 不只传容器名就宣称链路可用.
5. 无信箱时保留交付包链接可点的路径, 不新增容器 URL 挂载文件或回程队列. 不因 host 有桌面就把浏览器开到 host.
6. LAN 地址优先已确认值, 无法确定时交给 agent 问用户, 不把首个网卡地址直接视为可用地址.
7. 确保未来新容器实际取得本轮更新的规则. 镜像已有 EXPOSE 只证明不用增加该声明, 不证明 skill 分发无需重建: skills 为 base 构建期 COPY, 母本为 birth 留档挂载. 按实际修改对象处理分发, 如需镜像更新沿用既有流程并与 M08/M09 协调, 不动现有容器.

## 验证与依赖

- 六项已确认验收逐条见账本 D011; TC-004 结合 D012 验证两台设备同时取信和用户换电脑后仍开在正确设备.
- 端口发布/STATE/双 URL/单实例复用不依赖信箱运行. M03 实施已关闭, M05 使用已交付的信箱能力跑通查网址/回话/代开, 不以缺省轮询路由冒充当前设备识别.
- 自动化检查经 swt/present 的公开命令与 HTTP 观察行为; 设备位置和跨机访问由 M09 新容器真跑验证. 未跑过的路径保留为待验收, 不记通过.
- 页面入口在 birth 时尚未就绪, 只交付入口, 不提前自动打开. 分享 URL 不保证跨容器重建稳定.

移交与软依赖:
- 吸收旧图 MILESTONE-14 第 2 项 (展示端口预留, D053 回环方案已被 0.0.0.0 直达超集取代, D053 已标替代).
- 软依赖旧图 MILESTONE-14 第 3 项 (LAN IP 选取, VPN 在场取错): 交付包局域网 URL 用址须正确; 实施时若该项未修, 顺带修或复用其修法.
