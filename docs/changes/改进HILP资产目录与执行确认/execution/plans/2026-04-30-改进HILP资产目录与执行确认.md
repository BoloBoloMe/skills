# 改进 HILP 资产目录与执行确认执行计划

HILP design asset_ref: `stage-3/design-choice@v2 [state=approved｜中文状态=已批准]`
HILP blueprint asset_ref: `stage-4-5/implementation-blueprint@v1 [state=approved｜中文状态=已批准]`
HILP execution handoff asset_ref: `stage-6/execution-handoff@v1 [state=archived｜中文状态=已归档]`
执行确认状态: waiting-for-user-confirmation
禁止越界项:
- 不迁移、重命名或删除旧 `docs/hilp/<变更概述>/` 历史资产。
- 不迁移、重命名或删除旧 `docs/changes/<变更概述>/execution/plans/` 历史计划。
- 不改变 HILP 设计、蓝图、执行交接的审批语义。
- 不引入 planning 式执行计划审批状态机。
- 不修改本计划文件范围之外的技能文档或业务代码。
目标: 更新人在回路 planning 与 execution 两个 skill 的新资产路径规则，并加入执行计划写入后的用户确认门。
执行约束:
- 本计划写入后停止，等待用户明确确认当前计划文件。
- 用户未确认当前计划文件前，不修改目标文件，不派发 agent，不运行实现步骤。
- 执行时仅修改文件结构清单列出的文件。

## HILP 入口接收摘要

- 设计资产：[02-方案设计_design-choice@v2.md](../../planning/assets/02-方案设计_design-choice@v2.md)，状态为已批准。
- 蓝图资产：[03-实施蓝图_implementation-blueprint@v1.md](../../planning/assets/03-实施蓝图_implementation-blueprint@v1.md)，状态为已批准，确定性检查已通过。
- 执行交接：[05-执行交接_execution-handoff@v1.md](../../planning/assets/05-执行交接_execution-handoff@v1.md)，owner_skill 为 `hilp-execution-handoff`，执行入口检查为无阻断项。
- 执行范围：整包。
- 当前工作区：`D:/Workspace/skills`。

## 文件结构和文件职责

| 文件路径 | 文件职责 | 本次修改职责 |
|---|---|---|
| `human-in-loop-planning/SKILL.md` | 规划 skill 总入口、资产落盘规则和输出纪律 | 将新规划资产根目录改为 `docs/changes/<变更概述>/planning/`，保留旧路径历史兼容说明 |
| `human-in-loop-planning/references/handoff-contracts.md` | 规划阶段交接契约、资产路径模板和元数据模板 | 同步规划资产保存位置和 `asset_path` 模板 |
| `human-in-loop-planning/references/event-action-rules.md` | 规划事件动作规则、默认保存目录和元数据模板 | 同步默认保存目录和 `asset_path` 模板 |
| `human-in-loop-execution/SKILL.md` | 执行 skill 总入口、路由规则和输出纪律 | 将执行计划路径改为 `docs/changes/<变更概述>/execution/plans/`，加入执行计划确认阶段 |
| `human-in-loop-execution/references/execution-routing.md` | 执行阶段路由规则 | 加入计划已保存但用户未确认时停在执行计划确认阶段的规则 |
| `human-in-loop-execution/references/writing-plans.md` | 执行计划编写规则 | 将计划保存路径改为 `docs/changes/<变更概述>/execution/plans/`，要求计划写入后停止并等待用户确认 |
| `human-in-loop-execution/references/executing-plans.md` | inline 执行计划规则 | 增加“用户已明确确认当前计划文件”作为输入契约 |
| `human-in-loop-execution/references/subagent-driven-development.md` | subagent 执行规则 | 增加“用户已明确确认当前计划文件”作为输入契约 |
| `README.md` | 仓库目录和技能说明 | 同步 `docs/hilp/planning/` 与 `docs/hilp/execution/` 目录说明 |

## 任务 1：更新 planning skill 新资产根目录

### 步骤 1.1：修改 `human-in-loop-planning/SKILL.md`
- 文件路径：`human-in-loop-planning/SKILL.md`
- 目标：把新规划资产保存位置统一改为 `项目根目录/docs/hilp/planning/变更概述/`。
- 失败测试或验证命令：
  ```bash
  rg -n "项目根目录/docs/hilp/变更概述|asset_path: <project-root>/docs/hilp/<change-summary>" human-in-loop-planning/SKILL.md
  ```
- 预期初始输出：至少命中 `项目根目录/docs/hilp/变更概述/`。
- 最小实现：
  1. 将资产落盘规则代码块第一行改为 `项目根目录/docs/hilp/planning/变更概述/`。
  2. 在“旧命名资产不迁移、不重命名”所在段落保留该兼容语义，并补入旧 `docs/hilp/<变更概述>/` 历史资产仍可读取的说明。
- 回归验证：
  ```bash
  rg -n "项目根目录/docs/hilp/planning/变更概述|docs/hilp/<变更概述>" human-in-loop-planning/SKILL.md
  ```
- 预期回归输出：命中新规划路径和旧路径历史兼容说明。
- 变更记录：记录为 `planning-asset-root/SKILL.md`。

### 步骤 1.2：修改 `human-in-loop-planning/references/handoff-contracts.md`
- 文件路径：`human-in-loop-planning/references/handoff-contracts.md`
- 目标：同步交接契约中的规划资产根目录和元数据模板。
- 失败测试或验证命令：
  ```bash
  rg -n "项目根目录/docs/hilp/变更概述|asset_path: <project-root>/docs/hilp/<change-summary>" human-in-loop-planning/references/handoff-contracts.md
  ```
- 预期初始输出：命中保存位置代码块和元数据模板。
- 最小实现：
  1. 将“资产保存位置”代码块第一行改为 `项目根目录/docs/hilp/planning/变更概述/`。
  2. 将元数据模板中的 `asset_path: <project-root>/docs/hilp/<change-summary>/assets/<file-name>.md` 改为 `asset_path: <project-root>/docs/hilp/planning/<change-summary>/assets/<file-name>.md`。
  3. 在资产保存位置说明中补入旧 `docs/hilp/<变更概述>/` 历史资产不迁移且可兼容读取。
- 回归验证：
  ```bash
  rg -n "docs/hilp/planning/<change-summary>|项目根目录/docs/hilp/planning/变更概述|docs/hilp/<变更概述>" human-in-loop-planning/references/handoff-contracts.md
  ```
- 预期回归输出：命中新规划路径、元数据模板和旧路径兼容说明。
- 变更记录：记录为 `planning-asset-root/handoff-contracts.md`。

### 步骤 1.3：修改 `human-in-loop-planning/references/event-action-rules.md`
- 文件路径：`human-in-loop-planning/references/event-action-rules.md`
- 目标：同步事件规则中的默认保存目录和元数据模板。
- 失败测试或验证命令：
  ```bash
  rg -n "项目根目录/docs/hilp/变更概述|asset_path: <project-root>/docs/hilp/<change-summary>" human-in-loop-planning/references/event-action-rules.md
  ```
- 预期初始输出：命中默认保存目录和元数据模板。
- 最小实现：
  1. 将规则 D 中默认保存目录改为 `项目根目录/docs/hilp/planning/变更概述/`。
  2. 将元数据模板中的 `asset_path: <project-root>/docs/hilp/<change-summary>/assets/<file-name>.md` 改为 `asset_path: <project-root>/docs/hilp/planning/<change-summary>/assets/<file-name>.md`。
  3. 在规则 D 的旧资产兼容条目中补入旧 `docs/hilp/<变更概述>/` 历史资产仍可读取。
- 回归验证：
  ```bash
  rg -n "项目根目录/docs/hilp/planning/变更概述|docs/hilp/planning/<change-summary>|docs/hilp/<变更概述>" human-in-loop-planning/references/event-action-rules.md
  ```
- 预期回归输出：命中新规划路径、元数据模板和旧路径兼容说明。
- 变更记录：记录为 `planning-asset-root/event-action-rules.md`。

## 任务 2：更新 execution skill 计划路径和确认门

### 步骤 2.1：修改 `human-in-loop-execution/SKILL.md`
- 文件路径：`human-in-loop-execution/SKILL.md`
- 目标：增加执行计划确认阶段，并修改计划保存路径。
- 失败测试或验证命令：
  ```bash
  rg -n "docs/human-in-loop-execution/plans|没有执行计划时进入执行计划阶段|有独立任务、无共享文件、平台支持 subagent 时进入 subagent 执行阶段|无 subagent、任务强耦合或平台限制时进入 inline 执行阶段" human-in-loop-execution/SKILL.md
  ```
- 预期初始输出：命中旧计划路径和直接从计划进入执行的路由规则。
- 最小实现：
  1. 在阶段名称列表中，在“执行计划阶段”之后增加“执行计划确认阶段”。
  2. 将路由规则改为四条顺序规则：
     - 没有执行计划时进入执行计划阶段。
     - 执行计划已保存但用户未明确确认当前计划文件时进入执行计划确认阶段，只输出计划链接、自检结果、推荐执行方式和确认请求。
     - 用户明确确认当前计划文件后，有独立任务、无共享文件、平台支持 subagent 时进入 subagent 执行阶段。
     - 用户明确确认当前计划文件后，无 subagent、任务强耦合或平台限制时进入 inline 执行阶段。
  3. 将输出纪律中的计划保存路径改为 `docs/changes/<变更概述>/execution/plans/<yyyy-mm-dd>-<任务概括>.md`。
  4. 在输出纪律中增加一句：执行计划写入后必须停止，等待用户明确确认当前计划文件后才允许执行任务。
- 回归验证：
  ```bash
  rg -n "执行计划确认阶段|docs/hilp/execution/plans|用户明确确认当前计划文件|等待用户明确确认" human-in-loop-execution/SKILL.md
  ```
- 预期回归输出：命中新增阶段、新计划路径和确认门规则。
- 变更记录：记录为 `execution-confirmation-gate/SKILL.md`。

### 步骤 2.2：修改 `human-in-loop-execution/references/execution-routing.md`
- 文件路径：`human-in-loop-execution/references/execution-routing.md`
- 目标：使执行路由在计划保存后停在确认阶段。
- 失败测试或验证命令：
  ```bash
  rg -n "没有计划先写执行计划；已有计划按 subagent 或 inline 执行|阶段名固定为：执行入口检查阶段、执行计划阶段、subagent 执行阶段" human-in-loop-execution/references/execution-routing.md
  ```
- 预期初始输出：命中旧路由优先级和旧阶段名列表。
- 最小实现：
  1. 在输入契约中增加 `当前计划确认状态：未生成计划、等待用户确认、用户已确认当前计划文件之一。`
  2. 将路由优先级第 3 项改为：当前任务状态为没有计划时先写执行计划；计划已保存但用户未明确确认当前计划文件时进入执行计划确认阶段；用户已明确确认当前计划文件后按 subagent 或 inline 执行。
  3. 将固定阶段名列表增加“执行计划确认阶段”。
  4. 在检查清单中增加“已确认用户是否明确确认当前计划文件”。
- 回归验证：
  ```bash
  rg -n "执行计划确认阶段|当前计划确认状态|用户已明确确认当前计划文件|已确认用户是否明确确认当前计划文件" human-in-loop-execution/references/execution-routing.md
  ```
- 预期回归输出：命中输入契约、路由规则、阶段名和检查清单。
- 变更记录：记录为 `execution-confirmation-gate/execution-routing.md`。

### 步骤 2.3：修改 `human-in-loop-execution/references/writing-plans.md`
- 文件路径：`human-in-loop-execution/references/writing-plans.md`
- 目标：更新计划保存路径，固定计划头增加确认状态，并要求写入计划后停止。
- 失败测试或验证命令：
  ```bash
  rg -n "docs/human-in-loop-execution/plans|HILP execution handoff asset_ref:|输出已保存计划路径" human-in-loop-execution/references/writing-plans.md
  ```
- 预期初始输出：命中旧计划路径、固定计划头和旧输出契约。
- 最小实现：
  1. 将计划保存路径改为 `docs/changes/<变更概述>/execution/plans/<yyyy-mm-dd>-<任务概括>.md`。
  2. 在固定计划头中 `HILP execution handoff asset_ref:` 之后增加 `执行确认状态: waiting-for-user-confirmation`。
  3. 在执行规则中增加：计划保存后必须停止，不得执行任务、修改目标文件、派发 agent 或运行实现步骤。
  4. 将输出契约改为输出已保存计划路径、任务列表摘要、绑定的 HILP asset_ref、禁止越界项、自检结果、推荐执行方式和用户确认请求。
- 回归验证：
  ```bash
  rg -n "docs/hilp/execution/plans|执行确认状态: waiting-for-user-confirmation|计划保存后必须停止|用户确认请求" human-in-loop-execution/references/writing-plans.md
  ```
- 预期回归输出：命中新计划路径、固定计划头、停止规则和确认请求。
- 变更记录：记录为 `execution-plan-root/writing-plans.md` 与 `execution-confirmation-gate/writing-plans.md`。

### 步骤 2.4：修改 `human-in-loop-execution/references/executing-plans.md`
- 文件路径：`human-in-loop-execution/references/executing-plans.md`
- 目标：禁止 inline 执行在用户确认计划前启动。
- 失败测试或验证命令：
  ```bash
  rg -n "已保存执行计划路径|当前分支或工作区由用户指定且允许执行" human-in-loop-execution/references/executing-plans.md
  ```
- 预期初始输出：命中输入契约位置。
- 最小实现：
  1. 在输入契约中 `已保存执行计划路径。` 后增加 `用户已明确确认当前执行计划路径。`
  2. 在执行规则第 1 条前增加：若用户未明确确认当前执行计划路径，停止在执行计划确认阶段。
  3. 在检查清单中增加“用户已明确确认当前执行计划路径”。
- 回归验证：
  ```bash
  rg -n "用户已明确确认当前执行计划路径|执行计划确认阶段" human-in-loop-execution/references/executing-plans.md
  ```
- 预期回归输出：命中输入契约、执行规则和检查清单。
- 变更记录：记录为 `execution-confirmation-gate/executing-plans.md`。

### 步骤 2.5：修改 `human-in-loop-execution/references/subagent-driven-development.md`
- 文件路径：`human-in-loop-execution/references/subagent-driven-development.md`
- 目标：禁止 subagent 执行在用户确认计划前启动。
- 失败测试或验证命令：
  ```bash
  rg -n "已保存执行计划路径|subagent 可用性确认" human-in-loop-execution/references/subagent-driven-development.md
  ```
- 预期初始输出：命中输入契约位置。
- 最小实现：
  1. 在输入契约中 `已保存执行计划路径。` 后增加 `用户已明确确认当前执行计划路径。`
  2. 在执行规则第 1 条前增加：若用户未明确确认当前执行计划路径，不派发 subagent，停止在执行计划确认阶段。
  3. 在检查清单中增加“用户已明确确认当前执行计划路径”。
- 回归验证：
  ```bash
  rg -n "用户已明确确认当前执行计划路径|不派发 subagent|执行计划确认阶段" human-in-loop-execution/references/subagent-driven-development.md
  ```
- 预期回归输出：命中输入契约、执行规则和检查清单。
- 变更记录：记录为 `execution-confirmation-gate/subagent-driven-development.md`。

## 任务 3：同步仓库 README

### 步骤 3.1：修改目录树
- 文件路径：`README.md`
- 目标：让目录树展示 `docs/hilp/planning/`、`docs/hilp/execution/` 和 `docs/changes/<变更概述>/review/`。
- 失败测试或验证命令：
  ```bash
  rg -n "├── hilp/|docs/hilp/<变更概述>|HILP 规划资产保存到" README.md
  ```
- 预期初始输出：命中旧 `docs/hilp/` 目录说明或旧规划资产路径。
- 最小实现：
  1. 将 `docs/` 目录树下的 `hilp/` 改为包含 `hilp/planning/`、`hilp/execution/` 和 `review/` 的结构。
  2. 将 human-in-loop-planning 核心能力中“将规划资产落盘到 ...”改为 `docs/changes/<变更概述>/planning/`。
- 回归验证：
  ```bash
  rg -n "docs/hilp/planning|docs/hilp/execution|docs/hilp/<变更概述>" README.md
  ```
- 预期回归输出：命中 planning 和 execution 新路径，不命中旧规划默认路径。
- 变更记录：记录为 `repository-readme/tree-and-planning.md`。

### 步骤 3.2：修改资料与维护约定
- 文件路径：`README.md`
- 目标：同步资料目录和维护约定。
- 失败测试或验证命令：
  ```bash
  rg -n "docs/hilp/：HILP 规划链落盘资产|HILP 规划资产保存到 `docs/hilp/<变更概述>/`" README.md
  ```
- 预期初始输出：命中旧资料目录或旧维护约定。
- 最小实现：
  1. 将资料目录说明改为 `docs/hilp/planning/` 保存 HILP 规划链落盘资产。
  2. 增加 `docs/hilp/execution/` 保存 HILP 执行计划和执行层资产。
  3. 将维护约定中的 HILP 规划资产路径改为 `docs/changes/<变更概述>/planning/`。
- 回归验证：
  ```bash
  rg -n "docs/hilp/planning/：|docs/hilp/execution/：|docs/hilp/planning/<变更概述>" README.md
  ```
- 预期回归输出：命中新的资料目录说明和维护约定。
- 变更记录：记录为 `repository-readme/assets-convention.md`。

## 任务 4：运行验证与完成前检查

### 步骤 4.1：运行旧路径清理验证
- 文件路径：全目标文件集合。
- 目标：确认目标文档中不再保留旧默认路径规则。
- 失败测试或验证命令：
  ```bash
  rg -n "项目根目录/docs/hilp/变更概述|docs/human-in-loop-execution/plans|human-in-loop-execution/plans" human-in-loop-planning human-in-loop-execution README.md -g "*.md"
  ```
- 预期输出：无输出，退出码为 1。
- 最小实现：若有输出，修改对应目标文件，把旧默认路径改为蓝图指定的新路径；旧历史路径只允许以 `docs/hilp/<变更概述>` 形式出现在兼容说明中。
- 回归验证：重复执行本步骤命令。
- 预期回归输出：无输出，退出码为 1。
- 变更记录：记录为 `verification/old-path-cleanup`。

### 步骤 4.2：运行新路径与确认门验证
- 文件路径：全目标文件集合。
- 目标：确认新路径和确认门规则已经落到目标文件。
- 失败测试或验证命令：
  ```bash
  rg -n "docs/hilp/planning|docs/hilp/execution/plans|用户明确确认当前计划|等待用户确认" human-in-loop-planning human-in-loop-execution README.md -g "*.md"
  ```
- 预期输出：至少命中 planning 新路径、execution 新计划路径、用户明确确认当前计划、等待用户确认四类文本。
- 最小实现：若缺少任一类文本，回到任务 1、任务 2 或任务 3 的对应步骤补齐。
- 回归验证：重复执行本步骤命令。
- 预期回归输出：四类文本均出现。
- 变更记录：记录为 `verification/new-path-and-confirmation-gate`。

### 步骤 4.3：运行旧直通执行路由验证
- 文件路径：`human-in-loop-execution/references/execution-routing.md`。
- 目标：确认旧的“已有计划直接执行”路由文本被移除。
- 失败测试或验证命令：
  ```bash
  rg -n "没有计划先写执行计划；已有计划按 subagent 或 inline 执行" human-in-loop-execution -g "*.md"
  ```
- 预期输出：无输出，退出码为 1。
- 最小实现：若有输出，改为三段式路由：无计划写计划、计划未确认停在确认阶段、计划已确认后执行。
- 回归验证：重复执行本步骤命令。
- 预期回归输出：无输出，退出码为 1。
- 变更记录：记录为 `verification/no-direct-execution-after-plan`。

### 步骤 4.4：人工一致性检查
- 文件路径：
  - `human-in-loop-planning/SKILL.md`
  - `human-in-loop-planning/references/handoff-contracts.md`
  - `human-in-loop-planning/references/event-action-rules.md`
  - `human-in-loop-execution/SKILL.md`
  - `human-in-loop-execution/references/execution-routing.md`
  - `human-in-loop-execution/references/writing-plans.md`
  - `human-in-loop-execution/references/executing-plans.md`
  - `human-in-loop-execution/references/subagent-driven-development.md`
  - `README.md`
- 目标：确认规则一致且没有扩大蓝图范围。
- 失败测试或验证命令：人工阅读上述文件的修改片段。
- 预期输出：确认以下四项均成立：规划新路径一致、执行计划新路径一致、计划写入后停止、旧资产不迁移。
- 最小实现：若任一项不成立，只修改本计划文件结构清单内对应文件。
- 回归验证：再次人工阅读同一修改片段。
- 预期回归输出：四项均成立。
- 变更记录：记录为 `verification/manual-consistency`。

## 自检结果

- 蓝图覆盖：已覆盖 `planning-asset-root`、`execution-plan-root`、`execution-confirmation-gate`、`repository-readme` 四个切片。
- 占位符扫描：本计划不包含需要执行者补做规划判断的空步骤。
- 类型 / 方法签名一致性：本次仅修改 Markdown 文档，无代码类型或方法签名。
- 禁止越界项检查：计划未包含历史资产迁移、历史计划迁移、审批语义变更、执行计划状态机或业务代码修改。

## 推荐执行方式

推荐使用 inline 执行。理由：目标文件存在相邻规则文本和跨文件一致性要求，单会话顺序执行比并行派发更能避免相互覆盖。

## 用户确认门

本计划已保存。请用户明确确认当前计划文件后再执行：`docs/changes/改进HILP资产目录与执行确认/execution/plans/2026-04-30-改进HILP资产目录与执行确认.md`。
