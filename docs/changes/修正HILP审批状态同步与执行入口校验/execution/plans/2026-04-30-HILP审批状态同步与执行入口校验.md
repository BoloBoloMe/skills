# HILP 执行计划：修正审批状态同步与执行入口校验

HILP design asset_ref: `stage-3/design-choice@v1 [state=approved｜中文状态=已批准]`；文件链接：[02-方案设计_design-choice@v1.md](../../planning/assets/02-方案设计_design-choice@v1.md)
HILP blueprint asset_ref: `stage-4-5/implementation-blueprint@v1 [state=approved｜中文状态=已批准]`；文件链接：[03-实施蓝图_implementation-blueprint@v1.md](../../planning/assets/03-实施蓝图_implementation-blueprint@v1.md)
HILP execution handoff asset_ref: `stage-6/execution-handoff@v1 [state=archived｜中文状态=已归档]`；文件链接：[05-执行交接_execution-handoff@v1.md](../../planning/assets/05-执行交接_execution-handoff@v1.md)
禁止越界项:
- 不新增脚本。
- 不新增状态值或审批标记值。
- 不新增完整方案 B 的统一状态一致性门文件。
- 不正式引入方案 C 的新恢复事件模型。
- 不迁移、不批量修复历史规划资产。
- 不修改 `archive.md`、`blueprint.md`、`execution-handoff.md` 的完整入口门模型。
- 不修改业务代码。
目标:
- 在 planning 规则中明确人工批准通过后必须同步目标资产自身 front matter、正文状态摘要、manifest、review-pack 和 `_current/` 入口。
- 在跨阶段资产引用规则中保留 `manifest.md` 的 live manifest 与索引权威地位，同时要求绑定性下游核对实际资产文件自身状态。
- 在 execution 接收规则中明确读取实际设计与蓝图资产，核对 front matter、正文 `asset_ref`、执行交接引用和 manifest 状态；状态不一致时阻断，并输出固定恢复建议。
执行约束:
- 只修改四个 Markdown 规则文件：`human-in-loop-planning/SKILL.md`、`human-in-loop-planning/references/event-action-rules.md`、`human-in-loop-planning/references/handoff-contracts.md`、`human-in-loop-execution/references/hilp-handoff-intake.md`。
- `Human Approval Granted` 事件名保持不变。
- `manifest.md` 仍是 live manifest 和索引权威。
- 绑定性下游不得只信 manifest；必须核对实际资产文件自身状态。
- execution 接收规则不得获得规划资产写权限；只阻断并提示回到 HILP。
- 审批状态变化不递增内容版本，但必须同步同一版本资产的状态元数据与正文状态摘要。
- 本计划保存后停止；收到用户明确确认当前计划后，才修改目标文件、运行实现步骤和声明开始执行。

## 执行入口接收摘要

- 当前阶段：执行计划阶段。
- 当前工作区：`D:/Workspace/skills`。
- 执行范围：整包；覆盖执行交接资产列出的四个 Markdown 规则文件。
- 入口检查结论：已核对上游设计与蓝图资产为 `approved｜已批准`；执行交接资产 owner 为 `hilp-execution-handoff`，已落盘，执行入口检查为“无阻断项”。执行交接资产为 `archived｜已归档` 不阻断本次接收。
- 停止并回退条件：
  - 任一上游资产状态不再是已批准。
  - 执行计划需要新增蓝图外文件、脚本、状态值或恢复事件模型。
  - 发现必须修改四个目标文件之外的 skill 文档。
  - 发现旧歧义需要通过方案 B/C 才能消除。
  - 验证失败显示蓝图约束不充分。

## 文件结构和文件职责

| 文件 | 职责 | 本次修改边界 |
|---|---|---|
| `human-in-loop-planning/SKILL.md` | 规划协议总入口、资产落盘规则、审批与输出纪律 | 在资产落盘与审核完成规则中补强批准状态同步对象和失败约束 |
| `human-in-loop-planning/references/event-action-rules.md` | 事件触发、必需动作、状态变化规则 | 在 Human Approval Granted 和审核生命周期规则中列明原子同步对象、失败阻断、同版本状态同步 |
| `human-in-loop-planning/references/handoff-contracts.md` | 阶段交接、资产引用、manifest 与审核入口契约 | 在审核生命周期和跨阶段资产引用规则中改写“只信 manifest”的歧义，要求交叉校验实际资产 |
| `human-in-loop-execution/references/hilp-handoff-intake.md` | 执行交接接收入口校验 | 增加读取实际资产、状态一致性阻断和固定恢复提示 |

## 任务清单

### 任务 1：修改 `human-in-loop-planning/SKILL.md` 的审核完成规则

1. 打开 `human-in-loop-planning/SKILL.md`，定位“资产落盘规则”中以“审核完成后不得删除审核包”开头的段落。
2. 将该段批准通过规则改为：批准通过时必须同步同一版本正式资产的 front matter 状态字段、正文 `asset_ref`、正文“当前状态”、正文“当前是否需要审批”、`manifest.md`、对应 review-pack、`_current/当前待审.md`、`_current/当前已批准.md`。
3. 在同一段加入失败约束：任一同步对象写入失败时，不得声称审批状态已完成，不得进入蓝图或执行交接。
4. 保留原规则中“审核完成后不得删除审核包”“审核不通过时进入待修订”“内容修订必须生成下一版本正式资产和新的审核包”的语义。
5. 修改后运行验证命令：
   ```powershell
   rg -n "front matter|正文.*asset_ref|当前状态|当前是否需要审批|manifest|review-pack|当前待审|当前已批准|不得声称审批状态已完成" human-in-loop-planning/SKILL.md
   ```
6. 预期输出：命令退出码为 0；输出包含 `front matter`、`正文 asset_ref`、`当前状态`、`当前是否需要审批`、`manifest`、`review-pack`、`当前待审`、`当前已批准` 和 `不得声称审批状态已完成` 所在行。

### 任务 2：修改 `human-in-loop-planning/references/event-action-rules.md` 的批准事件和审核生命周期规则

1. 打开 `human-in-loop-planning/references/event-action-rules.md`，定位“事件 0：人工批准授予（Human Approval Granted）”。
2. 在“必需动作”中保留事件名和同一内容版本规则，并明确批准通过原子同步对象清单：目标正式资产 front matter、目标正式资产正文 `asset_ref`、正文“当前状态”、正文“当前是否需要审批”、`manifest.md`、审核包、`_current/当前待审.md`、`_current/当前已批准.md`。
3. 在“必需动作”中加入失败约束：任一同步对象写入失败时，不得声称审批状态已完成，不得按批准状态进入蓝图或执行交接。
4. 在“资产状态变化”中明确审批状态变化不递增内容版本，但必须同步同一版本资产自身状态元数据和正文状态摘要。
5. 定位“审核包生命周期规则”，将“`_current/当前已批准.md` 只列当前仍有效的已批准资产集合，不改变正式资产正文。”改写为：`_current/当前已批准.md` 只列当前仍有效的已批准资产集合；该入口不能替代目标正式资产正文状态同步，批准通过仍必须更新同一版本正式资产正文状态摘要。
6. 修改后运行验证命令：
   ```powershell
   rg -n "Human Approval Granted|front matter|正文.*asset_ref|当前状态|当前是否需要审批|manifest|review-pack|当前待审|当前已批准|不得声称审批状态已完成|不递增内容版本" human-in-loop-planning/references/event-action-rules.md
   ```
7. 预期输出：命令退出码为 0；输出包含 Human Approval Granted、原子同步对象、失败约束和不递增内容版本规则所在行。

### 任务 3：修改 `human-in-loop-planning/references/handoff-contracts.md` 的审核生命周期和跨阶段引用规则

1. 打开 `human-in-loop-planning/references/handoff-contracts.md`，定位“审核生命周期”。
2. 在“人工批准通过时”规则中明确同步对象：目标正式资产 front matter、目标正式资产正文 `asset_ref`、正文“当前状态”、正文“当前是否需要审批”、`manifest.md`、审核包、`_current/当前待审.md`、`_current/当前已批准.md`。
3. 在审核生命周期规则中加入：任一同步对象写入失败时，不得声称审批状态已完成；manifest 与目标正式资产自身状态不一致时，不得进入蓝图或执行交接。
4. 定位“跨阶段资产引用”的引用规则，将“`asset_ref` 中的状态优先从根目录 `manifest.md` 读取；兼容旧资产时从资产元数据和文件名读取。”改写为：先用根目录 `manifest.md` 定位当前资产和索引状态，再读取目标正式资产文件核对 front matter、正文 `asset_ref` 与正文状态摘要；绑定性下游只有在 manifest 与目标资产自身状态一致且均为 `approved｜已批准` 时才能推进。兼容旧资产时从资产元数据和文件名读取，但不得跳过实际文件状态核对。
5. 修改后运行验证命令：
   ```powershell
   rg -n "front matter|正文.*asset_ref|当前状态|当前是否需要审批|manifest|review-pack|当前待审|当前已批准|状态不一致|不得进入蓝图或执行交接|绑定性下游" human-in-loop-planning/references/handoff-contracts.md
   ```
6. 预期输出：命令退出码为 0；输出包含同步对象、状态不一致阻断、manifest 定位和实际资产核对规则所在行。

### 任务 4：修改 `human-in-loop-execution/references/hilp-handoff-intake.md` 的入口校验与恢复提示

1. 打开 `human-in-loop-execution/references/hilp-handoff-intake.md`，定位“执行规则”。
2. 将设计资产和蓝图资产校验规则扩展为：必须读取实际设计资产文件和实际蓝图资产文件，核对 front matter 状态、正文 `asset_ref` 状态、正文状态摘要、执行交接中的上游引用和 manifest 索引状态。
3. 在执行规则中加入状态不一致阻断：front matter、正文 `asset_ref`、正文状态摘要、执行交接引用和 manifest 任一不一致时，不得进入实现，不得自行修正规划资产。
4. 在失败输出规则中加入固定恢复建议：回到 HILP 变更重审，执行“审批状态一致性修复”；若用户批准事实明确，不生成新内容版本，只同步同一版本的状态字段和当前入口。
5. 保留“执行交接资产自身不要求已批准”“不得修改 HILP 规划资产状态”“执行交接资产为已归档不直接拒绝”的现有语义。
6. 修改后运行验证命令：
   ```powershell
   rg -n "读取实际|front matter|正文.*asset_ref|manifest|状态不一致|不得进入实现|不得自行修正|审批状态一致性修复|不生成新内容版本" human-in-loop-execution/references/hilp-handoff-intake.md
   ```
7. 预期输出：命令退出码为 0；输出包含实际文件校验、状态不一致阻断和固定恢复建议所在行。

### 任务 5：语义收敛与全量检索验证

1. 运行新增规则覆盖验证：
   ```powershell
   rg -n "front matter|正文.*asset_ref|当前状态|当前是否需要审批|审批状态一致性修复" human-in-loop-planning/SKILL.md human-in-loop-planning/references/event-action-rules.md human-in-loop-planning/references/handoff-contracts.md human-in-loop-execution/references/hilp-handoff-intake.md
   ```
2. 预期输出：命令退出码为 0；四个目标文件中至少一个命中项覆盖各自新增职责，execution 文件命中 `审批状态一致性修复`。
3. 运行旧歧义残留验证：
   ```powershell
   rg -n "不改变正式资产正文|asset_ref.*优先从根目录.*manifest" human-in-loop-planning/references/event-action-rules.md human-in-loop-planning/references/handoff-contracts.md
   ```
4. 预期输出：命令退出码为 1；不得出现旧句“`_current/当前已批准.md` 只列当前仍有效的已批准资产集合，不改变正式资产正文”和旧句“`asset_ref` 中的状态优先从根目录 `manifest.md` 读取”。
5. 运行禁止越界人工辅助检索：
   ```powershell
   rg -n "新增脚本|统一状态一致性门|恢复事件模型|archive\.md|blueprint\.md|execution-handoff\.md|业务代码" human-in-loop-planning/SKILL.md human-in-loop-planning/references/event-action-rules.md human-in-loop-planning/references/handoff-contracts.md human-in-loop-execution/references/hilp-handoff-intake.md
   ```
6. 预期输出：命令退出码为 1；四个目标文件不得新增方案 B/C、目标外文件或业务代码修改要求。

### 任务 6：变更记录与完成前验证准备

1. 汇总四个文件的变更摘要：
   - `human-in-loop-planning/SKILL.md`：批准状态同步对象和失败阻断。
   - `human-in-loop-planning/references/event-action-rules.md`：Human Approval Granted 原子同步、同版本状态同步、审核生命周期歧义收敛。
   - `human-in-loop-planning/references/handoff-contracts.md`：审核生命周期同步对象、manifest 定位加实际资产核对、状态不一致阻断。
   - `human-in-loop-execution/references/hilp-handoff-intake.md`：实际设计与蓝图资产读取、状态一致性校验、固定恢复建议。
2. 完成实现后进入代码审查阶段，按 Critical、Important、Minor 分类审查是否越界、是否破坏 manifest 索引权威、是否让 execution 修改规划资产、是否引入状态值或审批标记值。
3. 完成声明前重新运行任务 5 的三条验证命令，并在最终输出中列出命令、退出结果和输出摘要。
4. 本轮不执行 git commit；最终变更记录只写入对话完成摘要，除非用户另行要求提交。

## 自检结果

- 蓝图覆盖：已覆盖执行交接资产列出的四个目标文件和三个改动切片。
- 占位符扫描：本计划不含 TODO、TBD、“后续再定”、“类似上一步”、“写适当测试”、“补齐错误处理”、“按需实现”。
- 禁止越界项检查：计划不新增脚本、不新增状态值或审批标记值、不新增方案 B 文件、不引入方案 C 恢复事件模型、不迁移历史资产、不修改 `archive.md` / `blueprint.md` / `execution-handoff.md`、不修改业务代码。
- 推荐执行方式：单代理 inline 执行；四个 Markdown 文件存在跨文件语义一致性要求，不派发并行 agent。

## 当前暂停点

本计划已满足执行交接资产要求的“先生成执行计划并停止”。收到用户明确确认当前计划后，才能进入 inline 执行阶段并修改四个目标文件。
