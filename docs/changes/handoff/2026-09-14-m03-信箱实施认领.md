# 交接 2026-09-14: M03 信箱实施认领 (kimi k3, AFK 编码)

## 现状

M02 (信箱原型) 已关闭并推送 (`68220fd`): 用户真跑通过, 手感反馈正向, 结论与四条移交硬约束已写进 `roadmap/MILESTONE-02.md` 结论节. M03 (信箱实施) 阻塞已解除, 与 M04/M06 同在前沿互不阻塞. 本交接供认领 M03 的会话使用.

## 本轮产出 (其他文档没有的信息)

- **交付链路教训 (必知)**: 用户在宿主侧仓库检出里跑代码, 容器内工作区的改动**必须 commit + push 后用户 git pull 才能生效**. M02 期间 TUI 修复未推送导致用户连跑两轮旧代码, 误判 "原型跑不了". 凡是要用户真跑的东西: 先推送再交付运行命令; 交付物带版本标记 (如原型 TUI 的 `[r2 单键版]`) 让用户能自证跑的是新版.
- M02 关闭动作已全做完 (Milestone 头/结论/归档指针, ROADMAP 已关闭决策行+前沿+阻塞图 ✅, M03 头 "阻塞于" 已解除). 本会话无需再碰 M02.
- 原型 TUI 输入 bug 已修 (`858dafa`): cbreak 单键即响应, 问答期经 `Tui.prompt()` 切回行模式. 若 M03 写 e2e/冒烟涉及驱动该 TUI, 注意按键语义已变 (单键无回车).
- 环境: 本仓库已配 repo-local git 身份; 当前分支 `swt-cross-host-access`, 推送即交付 (git://127.0.0.1:9418/skills-v2); 用户经 herdr 远程连进容器操作, 交付命令要能在该形态下直接跑.
- M03 拆工序提示: 任务面大 (server 单体 + pi 扩展 + 取信脚本 + swt.py birth 集成 + e2e + skill 文档), `mailbox_logic.py` 是参考答案不是直接复用 — 正式版要加 SQLite 持久化 (D004 存续语义, 7 天滚动清理), admin 管理面走只听 127.0.0.1 的独立端口 (原型直接 in-process 调, 未演示).

## 必读推荐

- `docs/changes/swt-cross-host-access/roadmap/ROADMAP.md` — 会话入口: 目的地/前沿/迷雾/范围外/阻塞图, 先读.
- `docs/changes/swt-cross-host-access/roadmap/MILESTONE-03.md` — 本 Milestone 任务书 (状态/类型/问题节).
- `docs/changes/swt-cross-host-access/roadmap/MILESTONE-02.md` 结论节 — 原型结论 + 四条移交硬约束: 投信错误响应也签名 (D007 全响应签名) / 取信扩展忽略非 message 触发事件, 只对 message 事件 triggerTurn / 端口区间 38417-38426 本机首空闲 38417 / 持久化归 M03.
- `docs/changes/swt-cross-host-access/milestone-01/DECISIONS.md` — D001-D010 实施依据, 重点 D004 (4 类型/缺省路由/存续) / D005 (指令集降级) / D006 (key 作用域) / D007 (双向签名防重放) / D008 (长轮询/会话形态) / F006 (pi 扩展能力面).
- `docs/changes/swt-cross-host-access/prototypes/mailbox-loop/mailbox_logic.py` — 重写 server 的参考答案 (纯状态模型, 无 I/O); `server.py` / `fetch_loop.py` 供对照最小 HTTP 载体与取信循环.
- `docs/changes/swt-cross-host-access/recon/01-llm-channel.md` — pi 扩展能力面原始考察 (triggerTurn/file-trigger 骨架), 扩展部分直接输入.
- `docs/adr/0010-mailbox-centralized-web-monolith.md` + `0011-mailbox-exec-instruction-allowlist.md` — 架构约束, 与账本互证.
- `docs/language/UBIQUITOUS_LANGUAGE.md` — 术语对齐 (基础服务/信箱/取信会话/指令集).
- `pi/` 目录与 `sync-to-pi.py` — 扩展落点与同步方式; 改 pi 扩展前读 `pi/AGENTS.md`.

## 路线图

真实意图: 把 use-sandbox-worktree 的容器 GUI/内容无感访问能力完整固化进 skill 并真机验证 — 三类场景 (headed 浏览器过登录墙 / present 展示 / 容器 web 服务) × 两个访问位置 (宿主 Wayland 桌面 / 局域网其他 Wayland 设备), 零手工命令.

发展脉络:

1. 前序调研落盘: 跨机 GUI 可行性 (waypipe 实测 8 坑) + 容器 GUI 同机直通原理, 构成事实底座.
2. 方向侦查 (三子代理并行): 信箱通道 / 网页与网站 / waypipe 固化, 用户逐方向拍板.
3. 绘制 Roadmap; M01 信箱盘问关闭 (ADR 0010/0011).
4. M02 信箱原型交付 → 用户真跑跑通 (手感正向) → M02 关闭.
5. 本轮: M03 实施 (本会话) 与 M04 盘问 (另一会话, gpt6) 分头开展.

距目的地: M03 (信箱实施) → M08 (直飞实施, 依赖 M03) 一条线; M04 → M05 (网页实施) 一条线; M06 盘问 → M07 双机实测一条线; 三线汇入 M09 真机验收 → 目的地.
