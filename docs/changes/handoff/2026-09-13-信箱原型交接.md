# 交接 2026-09-13: 信箱原型 (probe 遍历 MILESTONE-02 进行中)

## 现状

probe 遍历会话: 前沿 M02/M04/M06 全 HITL, 按序认领 MILESTONE-02 (信箱原型, 状态已改 进行中). 原型 `mailbox-loop` 已交付并推送 (commit `855621e`), 冒烟全路径通过, 现停在 "等用户真跑原型并报手感" — 用户尚未运行. 新会话大概率从 "用户已跑/要跑原型" 或继续遍历开始.

## 本轮产出 (其他文档没有的信息)

- 原型四文件在 `docs/changes/swt-cross-host-access/prototypes/mailbox-loop/`: `mailbox_logic.py` (纯状态模型, 正式 swt-base-server 的参考答案) / `server.py` (HTTP 载体) / `fetch_loop.py` (阻塞取信脚本) / `tui.py` (终端界面).
- 关键设计取舍 (代码 docstring 之外、仅会话可知):
  - 取信链路全真: TUI 投信走真实 HTTP POST, 设备侧是真实子进程跑长轮询, 来信经触发文件唤醒 — 延迟是实测值不是模拟. 刻意如此, 为回答 "实际手感".
  - 投信方向的错误响应未签名, 是原型简化; 正式版按 D007 全响应签名.
  - 身份探测 (`/__identity__`) 事件也写进触发文件, 真实扩展应忽略非 message 事件 — 这是一条未成文的约定, M03 实施时要写进扩展.
  - 端口区间 38417-38426 本机实测首个空闲 = 38417.
- 冒烟实测结果 (脚本驱动, exit 0): 本机唤醒延迟 41ms/38ms/2ms; notify 处理; exec 白名单命中 → 直批; exec 任意命令 → 服务端降级 request → 用户批准; ct-evil 越权类型 → 403; 重放同 id → 被拦; 会话形态切换; 退出时触发文件与日志自动清理.
- 运行命令 (用户侧待执行): `cd docs/changes/swt-cross-host-access/prototypes/mailbox-loop && uv run python tui.py`
- 关 M02 还差的事实链: 用户真跑反馈 → prototype skill 规则 5 捕获 (结论记录 + 归档指针, 原型文件按 LOGIC 约定留在 docs/changes/ 由正式实现者参照) → Milestone 文件头改 已关闭 + ROADMAP 已关闭决策/前沿/连线图更新 + 提交 (`doc: 更新路线图 MILESTONE-02.md`).
- 环境: 本仓库已配 repo-local git 身份 (与历史提交同一作者), 新会话直接 commit 即可; 当前分支 `swt-cross-host-access`, 推送即交付 (git://127.0.0.1:9418/skills-v2).

## 必读推荐

- `docs/changes/swt-cross-host-access/roadmap/ROADMAP.md` — 会话入口: 目的地/前沿/迷雾/范围外/阻塞图, 每轮遍历先读它.
- `docs/changes/swt-cross-host-access/roadmap/MILESTONE-02.md` — 本 Milestone 的问题与完成标准 (状态: 进行中).
- `docs/changes/swt-cross-host-access/milestone-01/DECISIONS.md` — D001-D010, 原型落地依据; 关 M02 时摘要用. 重点是 D004 (4 类型/缺省路由) / D005 (指令集降级) / D006 (key 作用域) / D007 (签名防重放) / D008 (长轮询/会话形态) / F006 (pi 扩展能力面).
- `docs/adr/0010-mailbox-centralized-web-monolith.md` + `0011-mailbox-exec-instruction-allowlist.md` — M01 定稿的两条 ADR, 与账本互证.
- `docs/language/UBIQUITOUS_LANGUAGE.md` — 术语: 基础服务/信箱/取信会话/指令集, 讨论时对齐用词.
- `~/.agents/skills/prototype/SKILL.md` 与 `LOGIC.md` — 捕获流程 (规则 5) 与 "逻辑模块是参考答案/TUI 用后即焚" 的归档约定, 关 M02 时照此办.
- `docs/changes/swt-cross-host-access/recon/01-llm-channel.md` — pi 扩展能力面原始考察 (triggerTurn/file-trigger 骨架), M03 实施前的直接输入.

## 路线图

真实意图: 把 use-sandbox-worktree 的容器 GUI/内容无感访问能力完整固化进 skill 并真机验证 — 三类场景 (headed 浏览器过登录墙 / present 展示 / 容器 web 服务) × 两个访问位置 (宿主 Wayland 桌面 / 局域网其他 Wayland 设备), 零手工命令.

发展脉络:

1. 前序调研落盘: 跨机 GUI 可行性 (waypipe 实测 8 坑) + 容器 GUI 同机直通原理 + 交接文档, 构成事实底座.
2. 方向侦查 (三子代理并行): 信箱通道 / 网页与网站 / waypipe 固化, 用户逐方向拍板 (路线侦查结论已记入 ROADMAP 笔记).
3. 绘制 Roadmap; M01 信箱盘问关闭: 信箱与 llm-proxy 合并为 host 常驻 web 单体, exec 白名单+降级, 双向签名防重放 (ADR 0010/0011).
4. 本轮: M02 原型构建完成 + 冒烟通过, 等用户真跑.

距目的地: M02 关闭 (差用户一轮真跑) → M03 信箱实施 (AFK 编码) 与 M04/M06 盘问 (HITL) 可并行推进 → M05 网页实施 / M07 双机实测 → M08 直飞实施 (依赖 M03) → M09 真机验收 → 目的地. 未决迷雾 3 块 (Rust waypipe 重连 / noVNC 自动化 / URL 稳定性) 随前沿推进再定, 其中 URL 稳定性先由 M04 消化.
