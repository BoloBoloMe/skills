# 交接 2026-09-14: M04 网页/网站无感盘问 (gpt6, HITL)

## 现状

M02 (信箱原型) 已关闭并推送 (`68220fd`), 前沿为 M03/M04/M06 互不阻塞. M03 信箱实施由另一会话 (kimi k3) 认领; 本交接供开展 M04 盘问的会话使用. M04 是 HITL 盘问, 用户必须在线.

## 本轮产出 (其他文档没有的信息)

- **盘问边界 (关键)**: 大框架已在方向侦查阶段拍死 — O1+O9+O5 共形 (birth 补 `-p 8800`, 0.0.0.0 动态宿主端口; 8800 钉死为容器对外 web 端口, present/dev server 共用; web 服务直达局域网, 用户明示接受暴露取舍). 盘问是在框架内敲细节, **不重开方向**; 已淘汰项 (O4 pasta 自动转发 / O8 固定宿主端口 / O3 纯手工隧道) 不要复活, 淘汰理由见 ROADMAP 笔记 "路线侦查结论" 节.
- **"分享 URL 稳定性" 迷雾指定由 M04 优先消化**: 动态宿主端口每次重建会变, 链接失效; 消化不掉再立 Milestone (ROADMAP 未决迷雾节).
- 移交项: 旧图 MILESTONE-14 第 2 项 (D053 展示端口回环预留) 已由本路线吸收, 用户拍板 0.0.0.0 直达取代回环-only, D053 已标替代 — 按直达形态讨论, 不回退回环. 旧图 M14 第 3 项 (LAN IP 选取) 是 M05 软依赖, 涉及时需感知.
- 与信箱通道的叠加面 (代开浏览器/页面就绪通知) 要保持 "不依赖信箱也成立" — 信箱 (M03) 正在另一会话并行实施, 接口未定型前不要对它做硬假设.
- 环境: 当前分支 `swt-cross-host-access`, 推送即交付 (git://127.0.0.1:9418/skills-v2); 盘问结论落盘方式参照 M01 (决策账本 + 必要时 ADR, ROADMAP 已关闭决策行).

## 必读推荐

- `docs/changes/swt-cross-host-access/roadmap/ROADMAP.md` — 会话入口: 目的地/前沿/迷雾/范围外/阻塞图; 笔记节含术语约定 (设备侧/容器侧) 与关键不对称, "路线侦查结论" 节是 M04 的框架边界.
- `docs/changes/swt-cross-host-access/roadmap/MILESTONE-04.md` — 盘问议题清单 (移交项 / 8800 钉死细节 / birth -p 8800 + STATE 登记 + 双 URL / xdg-open 时机 / 多容器并存 URL 呈现 / 分享 URL 处置 / 信箱叠加面) 与完成标准.
- `docs/changes/swt-cross-host-access/recon/02-present-web.md` — 侦查底稿 (O1-O9 选项与取舍, 8800 硬缺口的查明过程).
- `docs/changes/swt-cross-host-access/milestone-01/DECISIONS.md` — M01 盘问结论落盘的格式参照 (盘问产出长什么样); D003/D004 涉及回信与通知, 与叠加面相关.
- `docs/adr/0005` (远程展示网段可读取舍) / `0006` (扁平并集) 与决策账本 D040 (6080 回环发布) / D053 (展示端口预留, 已标替代) — 旧决策上下文, 盘问涉及时对齐; 具体路径见 ROADMAP 笔记节.
- `docs/language/UBIQUITOUS_LANGUAGE.md` — 术语对齐 (sandbox-worktree/母体/常驻展示服务).

## 路线图

真实意图: 把 use-sandbox-worktree 的容器 GUI/内容无感访问能力完整固化进 skill 并真机验证 — 三类场景 (headed 浏览器过登录墙 / present 展示 / 容器 web 服务) × 两个访问位置 (宿主 Wayland 桌面 / 局域网其他 Wayland 设备), 零手工命令. 宿主无桌面会话 → 只走远程路径; 远程设备为 mac/Windows → noVNC 兜底.

发展脉络:

1. 前序调研落盘: 跨机 GUI 可行性 (waypipe 实测 8 坑) + 容器 GUI 同机直通原理, 构成事实底座.
2. 方向侦查 (三子代理并行): 信箱通道 / 网页与网站 / waypipe 固化, 用户逐方向拍板 (网页方向拍定 O1+O9+O5 共形 + 直达局域网).
3. 绘制 Roadmap; M01 信箱盘问关闭 (ADR 0010/0011).
4. M02 信箱原型交付 → 用户真跑跑通 → M02 关闭.
5. 本轮: M04 盘问 (本会话) 与 M03 实施 (另一会话) 分头开展.

距目的地: M04 → M05 (网页实施) 一条线; M03 (信箱实施) → M08 (直飞实施) 一条线; M06 盘问 → M07 双机实测一条线; 三线汇入 M09 真机验收 → 目的地. 未决迷雾 3 块 (Rust waypipe 重连 / noVNC 自动化 / URL 稳定性), URL 稳定性由本会话优先消化.
