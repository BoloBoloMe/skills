# Handoff 2026-09-11 (五) — F 轮: M10 全链验收重开

## 使命

M10 (use-sandbox-worktree SKILL.md 定稿 + 全链演练收口) 最终验收. 全链从零重跑:

**建 (birth) → 干 (用户 ssh/herdr 进容器改 `workflow/deliberate/SKILL.md`) → 推 (容器内 ff-push 回流母体) → 展示链 (容器内 present, host 侧 `podman port` 发现端口组装 URL) → 显示链 (noVNC + 隧道, headed 浏览器过登录墙) → 拆 (terminate, 母体存删用户自决)**

你的角色: host 侧编排 + 交付汇报 + 陪我验收, 不进容器干活. 一切 DECIDE 逐字转述等我答.

## 环境与代码现状 (全部已提交, v2 分支)

- 仓库 `/home/bolo/Workspace/skills` @ `c4131a8` 干净, 领先 origin 13 个提交未 push.
- 三层镜像就绪: base 2026.09.10-2 / display 2026.09.11-4 / skills 2026.09.11-3 (含 home 属主兜底, 镜像内零非 bolo 文件已实测).
- **演练现场未拆**: 容器 `swt-skill-optimization` 运行中 (旧代码 birth 的), 母体 `skills-v2-skill-optimization` 干净. 建议先 terminate 再从零 birth (全链要验 c4131a8 新代码, 含 hostname DECIDE).
- 网络模式已拍板: blacklist 无 --deny.
- 显示栈刚手动重拉过, display-check 全项 OK.

## 近三轮事故教训 (验收时重点盯)

1. **auth.json 注入制 (D046/F014)**: rootless uid_map 下 host 文件挂载进容器呈现 root 属主. 验收点: 容器内 `pi` 直接可用, auth.json 属主 bolo 600.
2. **home 属主兜底 (F013)**: 构建期/运行期以 root 写 bolo home 的三处残留已修. 验收点: `find /home/bolo ! -user bolo` 为空; herdr remote 双命令可用.
3. **hostname DECIDE (D047)**: birth 会多一个 DECIDE 问主机名, 候选 = 容器名, 转述给我拍板, 带 `--hostname <名>` 重跑.
4. **netns 绑定 (D048/F015)**: 多容器并存不再串台. 验收可选项: birth 后 `swt status` 的 netns 与 `podman inspect` SandboxKey 一致.

## 固定交付项 (birth/resume 每次齐发, 禁止遗漏)

ssh 双入口 (本机+局域网, 带端口) / 双凭证 (密码 sandbox + ed25519 私钥) / noVNC URL / 局域网隧道命令 / herdr remote 双命令. 细节以 `workflow/use-sandbox-worktree/SKILL.md` 诞生节为准.

## 关闭 M10 前欠的簿记

- DECISIONS.md 补记上/中批演练决策 (母体同级位置/bolo home 字面复刻/herdr 进 base/固定密码/env.conf/双入口/herdr 双命令必发/codex env_key 零秘密/base 烘配置/use-worktree 第零步) — 核对是否已齐, 缺则补.
- 展示链 known gap: swt 只映射 22+6080, present 8800 仅容器 IP 可达 — 我若要局域网访问展示页即是新发现, 带回问我.
- 关闭动作: MILESTONE-10 头改已关闭 → ROADMAP.md 更新 → 评估未决迷雾 (运行期新站点需求/D037 救场回访) → 提交.
- `sync-to-pi.py` 未跑 (用户自决); origin 未 push.

## 必读

1. `workflow/use-sandbox-worktree/SKILL.md` — 现行操作手册 (唯一权威).
2. `docs/changes/use-sandbox-worktree/DECISIONS.md` — D039-D048, F012-F015.
3. `docs/changes/use-sandbox-worktree/roadmap/MILESTONE-10.md` — 验收链定义.
4. 历史 handoff (背景, 登录墙独立容器形态已作废勿参考): `2026-09-09-m10-fix-session.md` / `2026-09-11-netns-multi-container-fix-complete.md`.
