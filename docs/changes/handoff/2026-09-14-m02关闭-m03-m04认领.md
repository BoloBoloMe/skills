# 交接 2026-09-14: M02 已关闭, M03/M04 分头认领

## 现状

M02 (信箱原型) 已关闭并推送 (`68220fd`): 用户真跑通过, 手感反馈 "挺好的", 结论与移交 M03 的事实已写进 `roadmap/MILESTONE-02.md` 结论节. 前沿变为 M03/M04/M06 互不阻塞. 用户指派: **新会话 A 认领 M03 信箱实施 (kimi k3, AFK 编码); 新会话 B 开展 M04 网页盘问 (gpt6, HITL)**.

## 本轮产出 (其他文档没有的信息)

- **交付链路教训 (重要, M03 会话必知)**: 用户在宿主侧仓库检出里跑代码, 容器内工作区的改动**必须 commit + push 后用户 git pull 才能生效**. 本轮 TUI 修复未推送导致用户连跑两轮旧代码, 误判为 "原型跑不了". 今后凡是要用户真跑的东西, 先推送再交付运行命令; 界面带版本标记 (本轮加 `[r2 单键版]`) 让用户能自证跑的是新版.
- TUI 输入修复 (`858dafa`): 原 `readline()` 整行读取 + 0.25s 全屏重绘, 表现为 "按键被吃/界面不变". 已改 cbreak 单键即响应, 问答期 (inject/process_next 的 input) 经 `Tui.prompt()` 切回行模式. pty 模拟全流程验证通过 (投信→唤醒 2ms→处理→退出).
- 用户经 herdr 远程连进容器操作 — 投信/处理全在远程会话完成, 手感不受影响, 侧面印证了远程会话形态可行 (但远程固定 tab 形态仍未真机验证, 留给 M07).
- M02 关闭动作已全做完: Milestone 头/结论节/归档指针, ROADMAP 已关闭决策行 + 前沿 + 阻塞图 ✅, M03 头 "阻塞于" 已解除. 新会话无需再碰 M02.

## 对会话 A (M03 实施) 的缺口提示

- M03 任务面大 (server 单体 + pi 扩展 + 取信脚本 + swt.py birth 集成 + e2e + skill 文档), 先读 MILESTONE-03.md 问题节再拆工序.
- MILESTONE-02.md 结论节的 "移交 M03 实施的事实" 四条是硬约束 (投信错误响应也签名 / 扩展忽略非 message 触发事件 / 端口区间 / 持久化 7 天清理归 M03).
- `mailbox_logic.py` 是参考答案不是直接复用: 正式版要加 SQLite 持久化, admin 管理面走只听 127.0.0.1 的独立端口.

## 对会话 B (M04 盘问) 的缺口提示

- M04 是 HITL 盘问, 用户必须在线; 议题清单在 MILESTONE-04.md, 方向侦查已拍的大框架 (O1+O9+O5 共形: 8800 钉死/0.0.0.0 动态宿主端口/web 直达局域网) 在 ROADMAP 笔记的 "路线侦查结论" 节, 盘问是在框架内敲细节, 不是重开方向.
- "分享 URL 稳定性" 迷雾指定由 M04 优先消化 (ROADMAP 未决迷雾节), 消化不掉再立 Milestone.
- 软依赖: 旧图 M14 第 3 项 (LAN IP 选取) 是 M05 软依赖, M04 若涉及需感知.

## 必读推荐

- `docs/changes/swt-cross-host-access/roadmap/ROADMAP.md` — 会话入口: 目的地/前沿/迷雾/范围外/阻塞图; M02 关闭行与路线侦查结论 (M04 的框架边界) 都在里面.
- `docs/changes/swt-cross-host-access/roadmap/MILESTONE-03.md` — 会话 A 的任务书 (状态/类型/问题节).
- `docs/changes/swt-cross-host-access/roadmap/MILESTONE-02.md` 结论节 — 会话 A 的原型结论与四条移交硬约束; 归档指针指向原型文件.
- `docs/changes/swt-cross-host-access/roadmap/MILESTONE-04.md` — 会话 B 的盘问议题清单与完成标准.
- `docs/changes/swt-cross-host-access/milestone-01/DECISIONS.md` — D001-D010, 会话 A 的实施依据 (重点 D004/D005/D006/D007/D008/F006).
- `docs/changes/swt-cross-host-access/prototypes/mailbox-loop/mailbox_logic.py` — 会话 A 重写 server 的参考答案 (纯状态模型, 无 I/O).
- `docs/changes/swt-cross-host-access/recon/01-llm-channel.md` — pi 扩展能力面原始考察 (triggerTurn/file-trigger 骨架), 会话 A 扩展部分的直接输入.
- `docs/changes/swt-cross-host-access/recon/02-present-web.md` — 会话 B 的侦查底稿 (O1-O9 选项与取舍).
- `docs/adr/0010-mailbox-centralized-web-monolith.md` + `0011-mailbox-exec-instruction-allowlist.md` — 会话 A 的架构约束, 与账本互证.
- `docs/language/UBIQUITOUS_LANGUAGE.md` — 两会话共用的术语对齐 (基础服务/信箱/取信会话/指令集).

## 路线图

真实意图: 把 use-sandbox-worktree 的容器 GUI/内容无感访问能力完整固化进 skill 并真机验证 — 三类场景 (headed 浏览器过登录墙 / present 展示 / 容器 web 服务) × 两个访问位置 (宿主 Wayland 桌面 / 局域网其他 Wayland 设备), 零手工命令.

发展脉络:

1. 前序调研落盘: 跨机 GUI 可行性 (waypipe 实测 8 坑) + 容器 GUI 同机直通原理 + 交接文档, 构成事实底座.
2. 方向侦查 (三子代理并行): 信箱通道 / 网页与网站 / waypipe 固化, 用户逐方向拍板.
3. 绘制 Roadmap; M01 信箱盘问关闭 (ADR 0010/0011).
4. M02 信箱原型交付 → 用户真跑 (本轮修复 TUI 输入 bug 后跑通, 手感正向) → M02 关闭.
5. 本轮交接: M03 实施 (kimi k3) 与 M04 盘问 (gpt6) 分头开展.

距目的地: M03 (信箱实施) → M08 (直飞实施, 依赖 M03) 一条线; M04 → M05 (网页实施) 一条线; M06 盘问 → M07 双机实测一条线; 三线汇入 M09 真机验收 → 目的地. 未决迷雾 3 块 (Rust waypipe 重连 / noVNC 自动化 / URL 稳定性), URL 稳定性先由 M04 消化.
