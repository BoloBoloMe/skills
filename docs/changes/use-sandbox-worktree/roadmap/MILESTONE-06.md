# 状态: 已关闭
# 类型: deliberate
# 阻塞于: 无

## 问题

镜像制备策略 (绘制会话拍板的方向: 镜像内容不钉死, host llm 按项目推导):

1. **依赖件分析流程**: host llm 如何分析项目得出容器内开发所需依赖件 (运行环境 / 构建工具 / harness=agent+扩展+skills / 浏览器 / 系统工具)
2. **版本号语义**: 镜像版本怎么编, 何时算新版本
3. **记录位置**: 优先 podman 原生 (借 MILESTONE-05 结论), 不够用再议外部登记
4. **"最合适" 匹配规则**: 已有多个镜像时如何选; 何种失配触发换版/新建
5. **约定降级归入本簇**: ~/AGENTS.md 引用的 ~/docs/, ~/Workspace/ 在容器内不存在 — 约定文档也是 harness 内容物, 由 host llm 制备时推导 (挂载或精简注入), 不落项目 git
