# 状态: 待处理
# 类型: task
# 阻塞于: MILESTONE-03

## 问题

五场景脚本抽取: 把 MILESTONE-03 实跑验证过的生命周期阶段抽取为 5 个 host 侧场景脚本入口: **诞生 / 恢复 / 查询 / 终结 / 换母体**.

背景: 脚本封装方向已确认 (把 D008 config 模板, D011 fail-closed 顺序等硬约束钉死在代码里, 防 llm 即兴漏项/乱序), 但首次完整流程跑通前不得冻结接口 (反方审查成立, 见 ../milestone-02-scripting-opposing-review.md). 本 Milestone 是验证后的正式抽取.

每个入口必须定义四件事:

- 状态前置条件 (什么状态下允许调用)
- 成功后状态 (可验证的断言)
- 失败清理责任 (中途失败留下什么, 谁收拾)
- 禁止操作 (如触犯 D010 单活动母体不变量的调用)

考察点 (反方审查列出的状态分支, 抽取时必须覆盖):

- 母体身份与复用: 当前目录是否 worktree / 已有容器探测 / 新建还是复用母体 / 单活动母体不变量检查 (D010)
- 恢复状态机: stale daemon 处理 / readiness 校验 / 中途失败可重入 (D011)
- 终结数据保护: 容器已停或 ssh 不可达时的脏检查路径 / "未 push" 的参照 ref / 强拆确认防误删 (D012)
- 共享母体: 多容器共存时查询与终结不能只看单容器本地状态 (D009)
- 全局 config: 既有配置处理 / 失败回滚 / git 版本兼容重验 (D008)
- 镜像版本: digest 比对提示挂进诞生入口 (D013)
- MILESTONE-03 checklist 里成熟的用户决策点转为脚本显式交互

完成判据: 5 个入口脚本各自四件事定义完整, 上述考察点逐项有落点, 脚本实跑通过.
