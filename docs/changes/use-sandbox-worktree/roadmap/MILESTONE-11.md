# 状态: 已关闭
# 类型: deliberate
# 阻塞于: MILESTONE-03

## 问题

五场景脚本抽取**方案**盘问 (用户在场参与): MILESTONE-03 实跑验证过的生命周期阶段, 如何抽取为 5 个 host 侧场景脚本入口: **诞生 / 恢复 / 查询 / 终结 / 换母体**.

背景: 脚本封装方向已确认 (把 D008 config 模板, D011 fail-closed 顺序等硬约束钉死在代码里, 防 llm 即兴漏项/乱序), 但首次完整流程跑通前不得冻结接口 (反方审查成立, 见 ../milestone-02-scripting-opposing-review.md). 本 Milestone 在 M03 验证事实在手后, 与用户敲定抽取方案; 实现由 MILESTONE-12 承担.

待盘问的取舍:

- 入口粒度: 严格五场景, 还是合并/拆分 (如查询并入其他入口, 换母体是否独立)
- 每个入口的四件事定义: 状态前置条件 / 成功后状态 / 失败清理责任 / 禁止操作
- 脚本与 skill 文档的职责边界: 哪些进脚本, 哪些留文档 (如原生报错译解表已归文档, D008)
- MILESTONE-03 checklist 里哪些用户决策点成熟为脚本显式交互

考察点 (反方审查列出的状态分支, 方案必须覆盖):

- 母体身份与复用: 当前目录是否 worktree / 已有容器探测 / 新建还是复用母体 / 单活动母体不变量检查 (D010)
- 恢复状态机: stale daemon 处理 / readiness 校验 / 中途失败可重入 (D011)
- 终结数据保护: 容器已停或 ssh 不可达时的脏检查路径 / "未 push" 的参照 ref / 强拆确认防误删 (D012)
- 共享母体: 多容器共存时查询与终结不能只看单容器本地状态 (D009)
- 全局 config: 既有配置处理 / 失败回滚 / git 版本兼容重验 (D008)
- 镜像版本: digest 比对提示挂进诞生入口 (D013)

完成标准: 盘问闭环, 抽取方案决策写入 DECISIONS.md.
