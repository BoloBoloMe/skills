# 状态: 开放
# 类型: task
# 阻塞于: 无
# 优先级: 高于后续所有里程碑 (含 MILESTONE-14) — 用户拍板 2026-09-13

## 问题

全量回归太慢: m04 ~2min / m07 ~2.5min / m09 ~10min (真实构建 base+display 镜像, chromium 下载大头) / m12 估计 20-40min (84 用例大量真 birth: 建容器+sshd+密钥注入+等 ssh+daemon+克隆, 串行). 每次 milestone 关账都付一次, 且越攒越慢.

设计红线: D036 精神不动 — 断言真实外部状态 (podman/nft/母体文件), 不靠 mock 替代系统行为. 优化只能动跑法与夹具组织, 不能降低证据等级.

## 方向 (2026-09-13 分析, 按收益/风险排序)

1. **分层跑法** (纯赚, 不改测试本体): 快层 = 纯逻辑/mock 单测 (秒级), 每次改动都跑; 慢层 = e2e, 按改动面选跑 (改 swt-vnc 才跑 m09, 改 birth 路径才跑 m12 birth 类); 全量只在 milestone 关账跑. 落地形态: pytest marker (如 `e2e`) + 各套件受影响面映射表 (文档化).
2. **birth 夹具合并** (最大提速点, 有真实权衡): m12 中只做只读断言的用例共享一次 birth, 不各生各的容器. 风险: 用例间隔离性下降, 一例弄脏状态污染后续 — 须划分"只读共享组"与"独占组", 写断言前明确哪些用例进哪组.
3. **并行** (不推荐, 仅评估后存档): 共享资源卡脖子 (nft 表全局 / 6080 / daemon 端口), 收益有限且易引入测试间干扰.

## 完成判据

- 分层跑法落地: 一条命令跑快层 (秒级出结果), 改动面→套件映射有文档.
- m12 全量耗时显著下降 (目标: 减半以上), 用例数与断言强度不减.
- 全量回归仍绿.
