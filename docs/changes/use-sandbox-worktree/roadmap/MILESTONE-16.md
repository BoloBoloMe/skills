# 状态: 已关闭
# 类型: task
# 阻塞于: 无

> 已关闭 (2026-09-20): m12 并行化落地 (xdist -n 4, 4m10s→~70s, 8+ 轮全绿零泄漏);
> L2 实测证伪 descope (层缓存跨轮存活, 暖构建 3m39s); 顺手修掉 BadStatusLine
> flake 根因 (U-018) 与 podman 锁耗尽 (U-020). 详见 ../milestone-16-parallel-m12.md.

## 问题

M15 收口后用户拍板继续压耗时 (2026-09-20):

- L1: m12 e2e 并行化 (pytest-xdist). 重开 M15 方向 3 — 原否决论据已部分过期
  (并行母体改造后 nft per-netns, 端口全动态), 剩余共享面: 6080 偏好竞争,
  固定容器名 5 处, net-firewall clear 作用面待核实.
- L2: m09/m07 镜像构建缓存 — 测试收尾清 fixture 镜像, 冷缓存首轮 ~10min
  (chromium 下载); 考察保留/复用空间.

红线不变 (D036): 断言真实外部状态, 不降低证据等级; 只动跑法与夹具组织.

## 完成判据

- m12 并行跑法落地且稳定 (连续多轮全绿), 耗时可测下降 (目标 ~2min 内).
- 串行跑法保留可用 (全量回归/关账路径不破).
- L2 有结论: 落地缓存复用或实测证明无可省.
