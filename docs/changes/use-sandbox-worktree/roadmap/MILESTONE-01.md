# 状态: 待处理
# 类型: deliberate
# 阻塞于: 无

## 问题

gate 设计三件套, 一次盘问拍完:

1. **读通道**: 真远端完全不对容器暴露, 读全走 gate 镜像 — 调研倾向是如此, 待确认.
2. **gate 服务形态**: git daemon (简单, 无认证, 靠网络访问控制兜底) vs ssh (git-over-ssh, 多一层身份门槛).
3. **gate 工作树的干净保障**: gate 工作树脏时 push 被拒 (自带保护但 agent 报错不透明), 编排侧谁来, 何时保持 gate 干净.

背景: 调研报告 §4.2/§4.3 (门禁实测矩阵全过; T7b 教训: 门禁由拓扑保证, real 仓绝不落在任何可写服务端点路径内; 生产建议 gate 用独立 clone).
