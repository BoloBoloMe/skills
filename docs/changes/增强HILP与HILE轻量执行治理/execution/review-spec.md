# Issues found

## Review

- Correct: 实际产品变更集中在 `human-in-loop-planning` 与 `human-in-loop-execution`，新增 reference 文件覆盖了 EU-001 至 EU-005 的主要协议对象；未看到新增 CLI、runtime、auto loop、dashboard、provider routing 或 Git worktree 自动化。

- Blocker: `human-in-loop-execution/SKILL.md:49` — 执行单个 `execution_unit` 时只要求读取 `execution-ledger.md` 与 `unit-summary.md`，`human-in-loop-execution/SKILL.md:94` 至 `human-in-loop-execution/SKILL.md:108` 的参考文件清单也没有 `references/execution-unit-intake.md`。这遗漏了执行计划要求的 HILE 入口加载职责：`docs/changes/增强HILP与HILE轻量执行治理/execution/plans/2026-05-02-增强HILP与HILE轻量执行治理.md:83` 明确要求 `human-in-loop-execution/SKILL.md` 加入 execution_unit intake、ledger、unit summary、failure forensics 加载与阶段规则。影响：EU-001/EU-003 依赖 `execution-unit-intake.md` 在执行当前单元前校验 `context_packet`、`allowed_files`、verification 与 stop conditions；入口不加载该规则会让 HILE 可能跳过逐单元接收门，进而违反“不得让 HILE 在执行阶段补做 HILP 蓝图判断”和 allowed_files 边界。修复方向：在 HILE `SKILL.md` 资源加载顺序中加入“执行单个 `execution_unit` 前先读取 `references/execution-unit-intake.md`，通过后再执行并使用 ledger/summary 记录”，并把该 reference 加入参考文件清单。

- Important: `human-in-loop-planning/SKILL.md:21` 至 `human-in-loop-planning/SKILL.md:32` — 规划入口只新增了 `references/execution-unit-schema.md`，没有把 `references/verification-contract.md` 与 `references/context-packet.md` 纳入加载规则或参考文件清单；但执行计划在 `docs/changes/增强HILP与HILE轻量执行治理/execution/plans/2026-05-02-增强HILP与HILE轻量执行治理.md:74` 要求 `human-in-loop-planning/SKILL.md` 补充 execution_unit、verification_contract、context_packet 参考文件加载规则。影响：EU-002 的 Must-haves Verification Ladder 与 EU-003 的 Context Packet 虽然新增了 reference 文件，但 HILP 总入口没有稳定触发它们，后续蓝图或执行交接可能只读 schema 而漏读验证契约与上下文包契约，导致 must_haves 或 context_packet 由执行/交接阶段临场补齐。修复方向：在 HILP `SKILL.md` 的模块参考文件清单和加载规则中加入 `references/verification-contract.md`、`references/context-packet.md`，并说明蓝图/执行交接涉及 `must_haves`、验证梯度或 `context_packet` 时必须读取。

- Important: `docs/changes/增强HILP与HILE轻量执行治理/execution/ledger.md:24` 至 `docs/changes/增强HILP与HILE轻量执行治理/execution/ledger.md:28` — EU-001 至 EU-005 的执行方式均记录为 `subagent-worker`，且存在 `docs/changes/增强HILP与HILE轻量执行治理/execution/subagent-EU-001.md:1` 等 subagent 执行报告；但执行交接在 `docs/changes/增强HILP与HILE轻量执行治理/planning/assets/05-执行交接_execution-handoff@v1.md:41` 至 `docs/changes/增强HILP与HILE轻量执行治理/planning/assets/05-执行交接_execution-handoff@v1.md:43` 指定“单代理、串行、逐 execution_unit 执行”，执行计划也在 `docs/changes/增强HILP与HILE轻量执行治理/execution/plans/2026-05-02-增强HILP与HILE轻量执行治理.md:381` 推荐 inline，理由是 subagent 会增加共享文件冲突和越界风险。影响：执行记录与已归档交接的执行模式不一致，属于执行过程偏离已批准 handoff；继续用这些 ledger/summaries 作为完成证据会削弱“单代理串行逐单元”的治理保证。修复方向：不要改写历史；若实际未使用 subagent，修正误记并移除或重命名误导性执行报告；若实际使用了 subagent，应追加偏差说明，将受影响单元标记为需要重审或回到 HILP 变更重审确认该执行方式是否可接受。
