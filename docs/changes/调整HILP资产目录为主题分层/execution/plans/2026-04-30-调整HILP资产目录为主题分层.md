# 调整 HILP 资产目录为主题分层执行计划

HILP design asset_ref: `stage-3/design-choice@v3 [state=approved｜中文状态=已批准]`；文件链接：[02-方案设计_design-choice@v3.md](../../../../../docs/changes/调整HILP资产目录为主题分层/planning/assets/02-方案设计_design-choice@v3.md)
HILP blueprint asset_ref: `stage-4-5/implementation-blueprint@v1 [state=approved｜中文状态=已批准]`；文件链接：[03-实施蓝图_implementation-blueprint@v1.md](../../../../../docs/changes/调整HILP资产目录为主题分层/planning/assets/03-实施蓝图_implementation-blueprint@v1.md)
HILP execution handoff asset_ref: `stage-6/execution-handoff@v1 [state=archived｜中文状态=已归档]`；文件链接：[05-执行交接_execution-handoff@v1.md](../../../../../docs/changes/调整HILP资产目录为主题分层/planning/assets/05-执行交接_execution-handoff@v1.md)
执行确认状态: waiting-for-user-confirmation
禁止越界项:
- 不得把 planning 的 `review-pack/` 移入 `review/`；`review-pack/` 仍属于 planning 资产。
- 不得重命名技能目录、内部模块名、asset_ref 或 HILP 概念名称。
- 不迁移、不删除旧 `docs/hilp/...`、`docs/changes/<变更概述>/review/...`、`docs/changes/<变更概述>/execution/plans/...` 历史资产。
- 执行计划确认门保持不变：计划写入后仍必须等待用户确认。
- 不修改蓝图文件范围之外的技能文档或业务代码。
目标:
- 将新产生的 planning 资产默认目录改为 `docs/changes/<变更概述>/planning/`。
- 将新产生的 execution 资产与执行计划默认目录改为 `docs/changes/<变更概述>/execution/` 与 `docs/changes/<变更概述>/execution/plans/<yyyy-mm-dd>-<任务概括>.md`。
- 将代码审查、协议审查、执行审查等审查结果文档默认目录改为 `docs/changes/<变更概述>/review/`，并要求输出可点击链接。
- 将仓库说明中的共同父目录改为 `docs/changes/`，保留旧目录为历史兼容读取来源。
执行约束:
- 仅编辑蓝图列出的 7 个 Markdown 目标文件和本计划文件。
- 实现顺序固定为 `planning-asset-root`、`execution-asset-root`、`review-result-root`、`repository-readme`。
- 旧资产目录只作为历史兼容说明保留；默认保存路径必须指向 `docs/changes/`。
- 不改变 HILP 审批包生命周期，不移动 `review-pack/`，不迁移历史资产。
- 执行本计划前必须获得用户对当前计划文件的明确确认。

## 文件结构与职责

| 文件 | 职责 | 本次改动边界 |
|---|---|---|
| [human-in-loop-planning/SKILL.md](../../../../../human-in-loop-planning/SKILL.md) | HILP planning 总入口、资产落盘规则、输出纪律。 | 替换新 planning 资产默认根目录与元数据路径模板；保留并标注旧目录历史兼容；保持 `review-pack/` 在 planning 结构内。 |
| [human-in-loop-planning/references/handoff-contracts.md](../../../../../human-in-loop-planning/references/handoff-contracts.md) | 规划阶段之间的交接契约、资产路径、链接规则和元数据模板。 | 同步新 planning 资产根目录、元数据路径模板和历史兼容说明；不改变交接关系和审批生命周期。 |
| [human-in-loop-planning/references/event-action-rules.md](../../../../../human-in-loop-planning/references/event-action-rules.md) | 事件动作规则、默认保存目录和资产元数据模板。 | 同步默认保存目录与元数据路径模板；保留状态转移和事件优先级规则。 |
| [human-in-loop-execution/SKILL.md](../../../../../human-in-loop-execution/SKILL.md) | HILP execution 总入口、执行路由和输出纪律。 | 将执行计划保存目录改为 `docs/changes/<变更概述>/execution/plans/`；保留执行计划确认门。 |
| [human-in-loop-execution/references/writing-plans.md](../../../../../human-in-loop-execution/references/writing-plans.md) | 执行计划编写契约。 | 将计划保存目录改为 `docs/changes/<变更概述>/execution/plans/`；保留计划写入后停止等待确认的规则。 |
| [human-in-loop-execution/references/code-review.md](../../../../../human-in-loop-execution/references/code-review.md) | 审查请求与反馈处理纪律。 | 增加审查结果保存目录、Windows 可写时间戳格式和可点击链接输出要求。 |
| [README.md](../../../../../README.md) | 仓库级目录树、技能概览、资料目录和维护约定。 | 将共同父目录说明改为 `docs/changes/`，把 planning、execution、review 描述聚合到同一变更主题目录。 |

## 任务 1：执行前范围基线检查

1. 在仓库根目录运行：

   ```bash
   rg -n "docs/hilp/planning|docs/hilp/execution/plans|docs/changes/<变更概述>/review/|docs/human-in-loop-execution/plans|human-in-loop-execution/plans|docs/changes|review-pack" human-in-loop-planning human-in-loop-execution README.md -g "*.md"
   ```

2. 预期输出：命中位置只用于定位路径说明；命中到蓝图列出的 7 个目标文件之外时，先判断该命中是否属于历史兼容说明或非默认路径示例。
3. 停止规则：命中到蓝图文件范围之外且必须修改该文件才能满足默认路径要求时，停止执行，回到 HILP 变更重审。
4. 最小实现：不编辑文件，只记录需要替换的默认路径位置。
5. 回归验证：无文件变更；继续任务 2。
6. 变更记录：在最终完成声明中摘要说明基线检查结果。

## 任务 2：实施 planning-asset-root

1. 编辑 [human-in-loop-planning/SKILL.md](../../../../../human-in-loop-planning/SKILL.md)：
   - 将资产落盘规则代码块的根目录改为 `项目根目录/docs/changes/变更概述/planning/`。
   - 将新资产根目录说明改为 `docs/changes/<变更概述>/planning/`。
   - 将 `asset_path` 元数据模板改为 `<project-root>/docs/changes/<change-summary>/planning/assets/<file-name>.md`。
   - 将旧 `docs/hilp/<变更概述>/`、旧 `docs/changes/<变更概述>/planning/` 表述保留为“历史兼容读取来源”。
   - 保持 `manifest.md`、`_current/`、`review-pack/`、`assets/` 位于 planning 资产结构内。
2. 编辑 [human-in-loop-planning/references/handoff-contracts.md](../../../../../human-in-loop-planning/references/handoff-contracts.md)：
   - 将资产保存位置代码块根目录改为 `项目根目录/docs/changes/变更概述/planning/`。
   - 将 `asset_path` 元数据模板改为 `<project-root>/docs/changes/<change-summary>/planning/assets/<file-name>.md`。
   - 将旧目录说明统一标注为“历史兼容读取来源”。
   - 保持审核包和当前入口的相对链接规则不变。
3. 编辑 [human-in-loop-planning/references/event-action-rules.md](../../../../../human-in-loop-planning/references/event-action-rules.md)：
   - 将默认保存目录改为 `项目根目录/docs/changes/变更概述/planning/`。
   - 将 `asset_path` 元数据模板改为 `<project-root>/docs/changes/<change-summary>/planning/assets/<file-name>.md`。
   - 保留事件优先级、状态转换和审核包生命周期规则。
4. 失败验证命令：

   ```bash
   rg -n "项目根目录/docs/hilp/planning/变更概述|<project-root>/docs/hilp/planning/<change-summary>/assets/<file-name>.md" human-in-loop-planning/SKILL.md human-in-loop-planning/references/handoff-contracts.md human-in-loop-planning/references/event-action-rules.md
   ```

5. 预期输出：退出码为 1 且无输出。
6. 回归验证命令：

   ```bash
   rg -n "项目根目录/docs/changes/变更概述/planning/|<project-root>/docs/changes/<change-summary>/planning/assets/<file-name>.md|review-pack/" human-in-loop-planning/SKILL.md human-in-loop-planning/references/handoff-contracts.md human-in-loop-planning/references/event-action-rules.md
   ```

7. 预期输出：三类 planning 目标文件均命中新 planning 根目录或新 `asset_path` 模板；`review-pack/` 仍在 planning 资产结构中出现。
8. 变更记录：记录 planning 默认路径已切换到 `docs/changes/<变更概述>/planning/`，旧路径仅保留为历史兼容读取来源。

## 任务 3：实施 execution-asset-root

1. 编辑 [human-in-loop-execution/SKILL.md](../../../../../human-in-loop-execution/SKILL.md)：
   - 将计划文件保存路径改为 `docs/changes/<变更概述>/execution/plans/<yyyy-mm-dd>-<任务概括>.md`。
   - 保留“执行计划写入后必须停止，等待用户明确确认当前计划文件后才允许执行任务”。
2. 编辑 [human-in-loop-execution/references/writing-plans.md](../../../../../human-in-loop-execution/references/writing-plans.md)：
   - 将计划保存路径改为 `docs/changes/<变更概述>/execution/plans/<yyyy-mm-dd>-<任务概括>.md`。
   - 保留固定计划头中的 `执行确认状态: waiting-for-user-confirmation`。
   - 保留“计划保存后必须停止，不得执行任务、修改目标文件、派发 agent 或运行实现步骤”。
3. 失败验证命令：

   ```bash
   rg -n "docs/hilp/execution/plans|docs/human-in-loop-execution/plans|human-in-loop-execution/plans" human-in-loop-execution/SKILL.md human-in-loop-execution/references/writing-plans.md
   ```

4. 预期输出：退出码为 1 且无输出。
5. 回归验证命令：

   ```bash
   rg -n "docs/changes/<变更概述>/execution/plans/<yyyy-mm-dd>-<任务概括>.md|执行计划写入后必须停止|waiting-for-user-confirmation" human-in-loop-execution/SKILL.md human-in-loop-execution/references/writing-plans.md
   ```

6. 预期输出：两个 execution 目标文件命中新执行计划路径；确认门相关句子仍存在。
7. 变更记录：记录 execution 计划目录已切换到变更主题下的 execution 子目录，确认门未改变。

## 任务 4：实施 review-result-root

1. 编辑 [human-in-loop-execution/references/code-review.md](../../../../../human-in-loop-execution/references/code-review.md)：
   - 在执行规则中增加：审查结果文档必须保存到 `docs/changes/<变更概述>/review/<审查内容概括-yyyy-mm-dd HH-MM-SS>.md`。
   - 写明时间戳使用 `yyyy-mm-dd HH-MM-SS`，不使用冒号。
   - 写明输出审查结论时必须提供审查结果文档的 Markdown 可点击链接。
   - 保持 Critical、Important、Minor 处理纪律和 YAGNI 检查不变。
2. 失败验证命令：

   ```bash
   rg -n "docs/changes/<变更概述>/review/" human-in-loop-execution/references/code-review.md
   ```

3. 预期输出：退出码为 1 且无输出。
4. 回归验证命令：

   ```bash
   rg -n "docs/changes/<变更概述>/review/<审查内容概括-yyyy-mm-dd HH-MM-SS>.md|Markdown 可点击链接|yyyy-mm-dd HH-MM-SS" human-in-loop-execution/references/code-review.md
   ```

5. 预期输出：`code-review.md` 命中新审查结果路径、时间戳格式和链接输出要求。
6. 变更记录：记录 review 结果目录已切换到变更主题下的 review 子目录。

## 任务 5：实施 repository-readme

1. 编辑 [README.md](../../../../../README.md)：
   - 将目录树中的 `docs/hilp/` 与 `docs/changes/<变更概述>/review/` 组合说明改为 `docs/changes/<变更概述>/planning|execution|review`。
   - 将 `human-in-loop-planning` 概览中的落盘目录改为 `docs/changes/<变更概述>/planning/`。
   - 将资料与资产目录改为：`docs/changes/` 是按变更主题聚合 planning、execution、review 的共同父目录。
   - 将维护约定中的审查报告和 HILP 规划资产保存位置改为 `docs/changes/<变更概述>/review/` 与 `docs/changes/<变更概述>/planning/`。
   - 明确旧 `docs/hilp/...`、`docs/changes/<变更概述>/review/...`、`docs/changes/<变更概述>/execution/plans/...` 仅为历史兼容资产读取来源，不迁移、不删除。
2. 失败验证命令：

   ```bash
   rg -n "docs/hilp/planning|docs/hilp/execution|docs/changes/<变更概述>/review/|docs/human-in-loop-execution/plans|human-in-loop-execution/plans" README.md
   ```

3. 预期输出：只允许出现明确标注“历史兼容”的旧路径说明；若旧路径仍作为默认保存目录出现，修正该行。
4. 回归验证命令：

   ```bash
   rg -n "docs/changes/<变更概述>/planning|docs/changes/<变更概述>/execution|docs/changes/<变更概述>/review|docs/changes/" README.md
   ```

5. 预期输出：README 命中新共同父目录和三类子目录。
6. 变更记录：记录仓库级说明已与技能文档路径一致。

## 任务 6：整体验证与人工检查

1. 运行蓝图验证命令 1：

   ```bash
   rg -n "docs/changes/<变更概述>/planning|docs/changes/<变更概述>/execution|docs/changes/<变更概述>/review" human-in-loop-planning human-in-loop-execution README.md -g "*.md"
   ```

   预期输出：至少命中 planning、execution、review 三类新路径。
2. 运行蓝图验证命令 2：

   ```bash
   rg -n "docs/human-in-loop-execution/plans|human-in-loop-execution/plans" human-in-loop-planning human-in-loop-execution README.md -g "*.md"
   ```

   预期输出：退出码为 1 且无输出。
3. 运行蓝图验证命令 3：

   ```bash
   rg -n "docs/changes/<变更概述>/review/|docs/hilp/planning|docs/hilp/execution/plans" human-in-loop-planning human-in-loop-execution README.md -g "*.md"
   ```

   预期输出：无输出，或仅命中明确标注“历史兼容”的语句；命中默认保存路径时修正后重跑。
4. 运行蓝图验证命令 4：

   ```bash
   rg -n "review-pack.*review/|review/.*review-pack" human-in-loop-planning human-in-loop-execution README.md -g "*.md"
   ```

   预期输出：不得出现要求把 `review-pack/` 放入 `review/` 的规则；命中语义为二者边界说明时人工判定为通过。
5. 人工检查 planning 目标文件：
   - [human-in-loop-planning/SKILL.md](../../../../../human-in-loop-planning/SKILL.md)
   - [human-in-loop-planning/references/handoff-contracts.md](../../../../../human-in-loop-planning/references/handoff-contracts.md)
   - [human-in-loop-planning/references/event-action-rules.md](../../../../../human-in-loop-planning/references/event-action-rules.md)

   检查点：`review-pack/` 仍位于 planning 资产结构内，未被放入 `review/`。
6. 人工检查 execution 目标文件：
   - [human-in-loop-execution/SKILL.md](../../../../../human-in-loop-execution/SKILL.md)
   - [human-in-loop-execution/references/writing-plans.md](../../../../../human-in-loop-execution/references/writing-plans.md)

   检查点：执行计划写入后等待用户确认的规则仍存在。
7. 人工检查 [human-in-loop-execution/references/code-review.md](../../../../../human-in-loop-execution/references/code-review.md)：
   - 检查点：审查结果路径为 `docs/changes/<变更概述>/review/`，并要求输出 Markdown 可点击链接。
8. 验证失败处理：任何验证失败先读取 [human-in-loop-execution/references/systematic-debugging.md](../../../../../human-in-loop-execution/references/systematic-debugging.md)，记录失败命令、实际输出、期望输出和最小修复，再重跑失败命令。
9. 变更记录：完成声明中列出每条验证命令、退出码和输出摘要。

## 任务 7：代码审查与收尾验证

1. 读取 [human-in-loop-execution/references/code-review.md](../../../../../human-in-loop-execution/references/code-review.md)，按 HILP asset_ref、禁止越界项和目标文件范围审查本次 diff。
2. 生成审查结果文档：`docs/changes/调整HILP资产目录为主题分层/review/调整HILP资产目录为主题分层代码审查-2026-04-30 HH-MM-SS.md`，其中 `HH-MM-SS` 使用执行审查时的本地时间。
3. 审查内容必须按 Critical、Important、Minor 分类；Critical 阻断继续推进，Important 修复后重验，Minor 记录但不掩盖阻断。
4. 完成前读取 [human-in-loop-execution/references/verification-before-completion.md](../../../../../human-in-loop-execution/references/verification-before-completion.md)，确认验证命令是本轮新鲜运行结果。
5. 完成声明必须包含：HILP 三类 asset_ref、计划文件链接、修改文件清单、验证命令、退出码、输出摘要、人工检查结论和审查结果链接。

## 自检结果

- 蓝图覆盖：已覆盖 `planning-asset-root`、`execution-asset-root`、`review-result-root`、`repository-readme` 四个切片。
- 文件范围：仅涉及蓝图列出的 7 个 Markdown 目标文件；本计划文件与后续审查结果文档属于执行与审查资产。
- 占位检查：计划内每个任务均给出精确文件路径、验证命令、预期输出、最小实现和回归验证。
- 禁止越界项检查：未要求移动 `review-pack/`，未要求重命名技能目录、内部模块名、asset_ref 或 HILP 概念名称，未要求迁移或删除历史资产。
- 执行确认门检查：本计划保存后停止，等待用户明确确认当前计划文件。
- 推荐执行方式：单代理顺序执行，原因是用户已选择单代理且跨文件一致性要求高。
