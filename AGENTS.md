# AGENTS.md 全局律令

## 0 效力顺序

- 用户当前明令高于本文件.
- 仓库内更近的 `AGENTS.md`,README,CONTRIBUTING,lint/test 配置,既有风格与团队规范,高于本文件.
- 不得以本文件为由扩大任务范围,重写无关模块或改变既有公共契约.

## 1 应答

- 结论在前,言简意明.
- 用户未要求复述时,文档已有内容不重复,只指明位置.
- 用户要求完整文件,完整代码或完整配置时,不得省略.
- 不确定处须明示,不得编造事实,命令,路径,接口或执行结果.
- 信息足够时,不反复追问；可在合理假设下继续,并说明假设.

## 2 环境

- Windows11.
- PowerShell7.
- Python托管于uv.
- 如非用户明令,行事循既定之器

## 3 注释

- 类,方法,测试应有必要注释.
- 注释只写契约,边界,约束,取舍,异常前提或测试意图.
- 名称自明,流程自明,传参与组装自明者,不注.
- 对外方法若有前提,例外,继承约束,幂等要求,权限要求或兼容承诺,须写明边界.
- TODO 须写明:待改约束,未改原因,触发条件.
- 不得为满足“有注释”而制造注释噪音.

## 4 审查

- 用户要求正式审查,保存审查结果或输出 review 文档时,审查须成报告.
- 用户仅作临时询问时,直接给出结论,问题与建议,不擅自落盘.
- 审查报告路径先从用户明令；无明令时,置于 `项目根/docs/review/<修改内容概括>.md`.
- 报告须包含:结论,问题清单,影响范围,建议改法,验证方式.

## 5 低复杂度代码

### 5.1 总则

- 主流程须清晰,业务语义须浮于表面,技术细节不得淹没主线.
- 不得为追逐复杂度数字而扩大边界,强造抽象,碎裂方法或滥设配置.
- 不得以简化之名损害正确性,安全性,权限,资金一致性,幂等性,数据一致性,兼容性与必要可观测性.
- 非用户明令,不因旁枝复杂而擅自大规模重构.
- 复杂度度量是约束,不是目的.

### 5.2 默认边界

新代码默认遵守:

- 普通方法 / 函数圈复杂度不超过 5.
- 核心业务编排方法 / 函数圈复杂度不超过 8.
- 嵌套不深于 3 层.
- 单个方法 / 函数参数不多于 5 个.
- 单个方法 / 函数不超过 50 行.
- 单个类 / 模块直接注入依赖不超过 7 个.
- 单个布尔表达式中的业务条件不超过 3 个.

若须越界,须说明:为何保留,风险何在,如何验证.

### 5.3 分支

新增 `if`,`else`,`switch`,`case`,`catch`,循环或复杂布尔表达式前,先判断:

> 此枝是否改变业务行为,数据状态,对外响应,异常策略,安全边界或可观测语义？

- 行为,状态,权限,安全,资金,风控,合规,幂等,持久化,外部调用,响应,补偿,告警,审计不同,方可分枝.
- 仅为日志文案,debug 信息,内部提示,非关键指标标签不同者,不得扰动主流程.
- 结果同,策略同,观测价值同者,须合为一流.
- 语义,排障,审计,告警,安全合规不同者,不得强合.

### 5.4 主流程

推荐结构:

```text
校验输入
构建上下文
执行业务动作
处理执行结果
发布事件或返回响应
```

- 校验,解析,状态处理,外部调用,补偿,观测,临时状态过多者,应下沉到具名方法.
- 下沉须有业务名,抽象须有语义根.
- 不得以下沉之名隐藏关键行为.

### 5.5 条件与状态

- 条件须少而明.
- 复杂 `&&` / `||` 应提取为语义方法.
- 不使用双重否定表达业务判断.
- 状态不得散落为字符串.
- 多状态组合须封为业务语义.
- 规则渐繁时,优先考虑语义方法,枚举行为,映射表,策略或状态机.
- 配置化规则仅在确有运行期变更,跨租户差异,运营维护或外部化管理需求时引入.

### 5.6 拆分与抽象

以下情形须考虑拆分:

- 方法越过复杂度边界.
- 嵌套深于 3 层.
- 分支丛生.
- 一法兼做校验,查询,计算,落库,通知,异常处理.
- 局部变量过多.
- 方法名已不能概括行为.

拆分须依业务语义而行,不以 `step1`,`handleData`,`doLogic`,`processInternal`,`common`,`helper` 掩盖职责.

若拆后参数更多,跳转更远,上下文更散,则宁可不拆.

### 5.7 日志,异常与可观测性

- 日志,异常,错误码,指标,trace,审计,皆循项目旧章.
- 无上下文,不自创格式；无体系据,不另立名目.
- 不得为低价值观测差异扰乱主流程.
- 不得为降低复杂度删去必要排障信息.
- `try/catch` 只围真正需要特殊处理之处.
- 异常不得吞；若须吞,必须是明确降级,且符合项目规范.
- 涉及资金,安全,权限,幂等,外部回调,数据一致性的异常,不得以普通日志替代明确处理.

### 5.8 测试

- 新代码须易测.
- 时间,随机数,外部服务,文件系统,网络,全局配置,须可注入,可替换或可隔离.
- 单法测试若需 mock 超过 5 个依赖,须审其职责是否过重.
- 修 bug 或增行为,须补核心路径测试.
- 若不能补测,须说明原因与验证方式.
- 测试应验证业务契约,不只验证实现细节.

### 5.9 修改范围

- 只改所需,不越边界.
- 可小理,不大拆.
- 可提取复杂条件,合并同果分支,缩小 `try/catch` 范围,补必要测试.
- 涉及跨模块边界,公共接口,持久化结构,错误码,状态机,权限,安全,资金,幂等性或外部 API,非用户明令,不主动大改.
- 当前任务外的复杂度,可指出,不擅自修复.

## 6 交付

- 当前任务内的复杂度,小步化解.
- 当前任务外的复杂度,不擅自扩大重构.
- 暂留复杂度时,说明:留何复杂,为何暂留,如何最小重构,如何验证.
- 能交付代码则交付代码,能交付文件则交付文件,不以空泛建议代替可用结果.
