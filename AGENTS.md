# 全局约束

## 回复约束

- 始终保持信息精炼
- 不要回复文档中已有的内容, 而是指引用户阅读文档

## 环境与用器

- Windows 11
- PowerShell 7
- Python 托管于 uv
- 行事循既定之器

## 源码注释

- 必注: 类,方法,测试
- 禁注: 代码与命名自明者,流程细枝,参数传递,返回组装
- 生产代码惟注三事: 契约与设因,所守约束,弃彼取此之因
- 代码块注惟五端: 外部协议,并发时序,性能取舍,历史兼容,业务边界
- 对外方法若有前提,例外,继承约束,则须注
- TODO 须具三事: 待改约束,未改之因,触发条件；缺一不得书
- 测试注四事: 所守约束,所防旧失,特设前提

## 代码审查

- 审查结果须写成审查报告
- 保存路径: 优先遵守用户的要求, 若无存储说明则默认存于 项目根/docs/review/<修改内容概括>.md

## 低复杂度代码编写约束

### 适用范围

本约束适用于 agent 编写新代码、修改代码和进行小规模重构时的默认行为。目标是让代码保持主流程清晰、业务行为明确、局部可改、易于测试。

本约束是全局默认规则，不覆盖用户的明确要求、仓库内更具体的 `AGENTS.md`、项目 README、CONTRIBUTING、lint/test 配置、已有代码风格和团队内部规范。发生冲突时，优先遵守更具体、更贴近当前仓库的规则。

除非用户明确要求重构，否则不要为了满足复杂度指标而扩大修改范围。复杂度问题超出当前任务范围时，应在回复中说明风险和建议，而不是擅自大改。

### 核心原则

1. 优先保持主流程简单、线性、可快速理解。
2. 只在业务行为确实不同的时候增加分支。
3. 不要为了日志文案、debug 信息、内部提示、非关键指标标签等低价值差异增加主流程复杂度。
4. 不要为了降低复杂度牺牲正确性、安全性、权限控制、资金一致性、幂等性、数据一致性或必要可观测性。
5. 相同结果的边界路径应优先合并；不同业务语义、不同排障价值或不同对外行为不应强行合并。
6. 复杂条件应提取为有业务语义的方法、对象、规则或配置。
7. 状态分支应优先用枚举行为、策略、状态机、映射表或清晰的语义方法表达，避免长 `if/else` 或 `switch/case` 链。
8. 新代码应尽量容易写单元测试，时间、随机数、外部服务和全局状态应可替换或可隔离。

### 默认复杂度预算

以下预算是新代码的默认目标，也是触发重构思考的信号。除非有明确业务理由，新写代码应尽量遵守。

| 对象                              |             默认限制 |
| --------------------------------- | -------------------: |
| 普通方法 / 函数的圈复杂度         |             不超过 8 |
| 核心业务编排方法 / 函数的圈复杂度 |            不超过 10 |
| 最大嵌套深度                      |          不超过 3 层 |
| 单个方法 / 函数参数数量           |          不超过 5 个 |
| 单个方法 / 函数长度               | 优先控制在 50 行以内 |
| 单个类 / 模块直接注入依赖数量     |      尽量不超过 7 个 |
| 单个布尔表达式中的业务条件数量    |      尽量不超过 3 个 |

允许超过预算，但必须满足：

1. 复杂度来自业务规则本身，而不是实现方式随意堆叠。
2. 已尝试使用语义方法、对象建模、策略、状态机、映射表或局部拆分降低复杂度。
3. 保留复杂度是为了正确性、安全性、幂等性、数据一致性、资金一致性、兼容性或必要异常处理。
4. 在最终回复中简要说明为什么保留该复杂度。

不要为了数字达标而制造更难理解的抽象、过度拆分、过度配置化或跨文件跳转。

### 新增分支前的判断

新增 `if`、`else`、`switch`、`case`、`catch`、循环或复杂布尔表达式前，先判断：

> 这个分支是否改变核心业务行为、数据状态、对外响应、异常策略、安全边界或可观测语义？

通常允许增加分支的情况：

- 返回不同业务结果。
- 更新不同数据状态。
- 影响权限、安全、资金、风控、合规、幂等性或兼容性。
- 影响持久化、消息发送、外部接口调用或对外 API 响应。
- 不同异常需要不同重试、补偿、降级或告警策略。
- 分支本身就是明确业务规则，且不能用更简单的数据结构表达。

通常不应增加主流程分支的情况：

- 只是为了不同日志文案。
- 只是为了不同 debug 信息或内部提示。
- 只是为了非关键指标维度。
- 多个失败原因最终业务处理完全相同，且没有不同排障、审计或告警价值。
- 条件差异不影响业务结果、数据状态、对外响应、异常策略或必要可观测性。

注意：业务处理结果相同，不代表观测语义一定相同。不得删除、合并或弱化对排障、审计、告警、安全、合规有价值的错误原因、日志字段、指标维度或错误码。

### 主流程结构要求

主流程代码应读起来像业务步骤说明，而不是细节堆叠。

推荐形态：

```text
validate input
build context
execute business action
handle result
publish event or return response
```

主流程中应避免直接塞入：

- 大段参数校验。
- 深层 JSON / map / dict 解析。
- 多状态组合判断。
- 多个外部服务调用细节。
- 复杂异常补偿。
- 重复日志、指标、审计等非核心观测分支。
- 大量临时变量和状态标记。

这些内容应优先封装到有明确业务语义的方法、对象或组件中。

### 条件表达式要求

1. 单个布尔表达式中的业务条件尽量不超过 3 个。
2. 如果条件包含多个业务概念，必须优先提取为语义方法。
3. 避免在主流程中混合复杂的 `&&` / `||`。
4. 避免双重否定。
5. 方法名应表达业务含义，例如 `canPay`、`canDeliver`、`isRetryable`、`shouldBlock`、`hasRequiredData`。

示例：

```text
不推荐：if user exists AND order is created AND amount > 0 AND payment method available AND risk passed
推荐：if order.canPayWith(paymentMethod, riskResult)
```

### 状态与规则表达

1. 避免散落的字符串状态判断。
2. 避免长 `if/else` 或 `switch/case` 状态链。
3. 多状态组合判断必须封装成业务语义。
4. 当状态规则持续增长时，优先考虑枚举行为、策略、状态机、映射表或配置化规则。
5. 不要为了“看起来高级”而引入不必要的状态机或策略模式；只有当规则增长、分支扩散或测试组合变多时才抽象。

### 方法拆分与抽象

应该拆分的信号：

- 方法超过默认预算，且包含多个业务步骤。
- 嵌套超过 3 层。
- 分支数量较多。
- 同时包含校验、查询、计算、落库、通知和异常处理。
- 局部变量过多，阅读时需要记住大量上下文。
- 方法名无法准确概括内部行为。

拆分应按业务语义进行，例如：

```text
validateRequest
buildPaymentContext
checkRisk
executePayment
handlePaymentResult
publishPaymentEvent
```

不要机械拆成 `step1`、`handleData`、`doLogic`、`processInternal` 这类无语义方法。

如果拆分后参数传递明显变多、跳转成本更高、上下文更难理解，则不要拆分。

### 日志、异常与可观测性

1. 优先遵守项目已有日志、异常、错误码、指标、trace、审计规范。
2. 在没有上下文时，不要自行创造新的日志格式、错误码体系、指标命名或异常层级。
3. 不要为了低价值日志差异破坏主流程清晰度。
4. 也不要为了降低复杂度删除必要故障排查信息。
5. `try/catch` 范围应尽量小，只包裹真正需要特殊处理的代码。
6. 不要吞异常，除非这是明确的降级策略，并且符合项目规范。
7. `catch` 中如果包含状态更新、补偿、通知、审计或复杂返回值构造，应提取为语义方法。

### 可测试性要求

新代码应默认可测试：

1. 不要在业务方法内部直接创建难替换的外部依赖。
2. 时间、随机数、外部服务、文件系统、网络、全局配置等副作用应可注入、可替换或可隔离。
3. 单个方法测试如果需要 mock 超过 5 个依赖，应检查职责是否过重。
4. 单个方法如果需要覆盖大量分支组合，应检查是否可以拆分规则或提取策略。
5. 修复 bug 或新增行为时，应优先补充能覆盖核心路径的测试；如果无法补测试，应说明原因和建议的验证方式。

### 修改范围控制

1. 默认只修改完成当前任务所必需的代码。
2. 不要因为发现附近代码复杂就顺手大规模重构。
3. 可以做低风险的局部整理，例如提取明显复杂条件、合并完全相同结果的分支、缩小 `try/catch` 范围。
4. 涉及跨模块边界、公共接口、持久化结构、错误码、状态机、权限、安全、资金、幂等性或外部 API 的重构，必须谨慎；除非用户明确要求，否则只提出建议，不主动大改。
5. 如果必须超过复杂度预算，应在回复中说明原因、风险和验证方式。

### 输出代码后的自检

完成代码后，检查：

1. 主流程是否清晰、线性、容易阅读？
2. 是否引入了不必要的分支、嵌套或复杂布尔表达式？
3. 是否存在相同结果的边界路径可以合并？
4. 是否把不同业务语义、错误原因或可观测价值错误合并了？
5. 是否可以用语义方法、领域对象、策略、状态机或映射表降低认知复杂度？
6. 是否遵守当前仓库已有风格和规范？
7. 是否容易测试？
8. 是否保留了正确性、安全性、幂等性、数据一致性和必要可观测性？
9. 是否只修改了当前任务必要范围？
10. 如果复杂度超过预算，是否说明了业务必要性和验证方式？

> 此项工作不需要遵守代码审查结果须落盘的约束, 自检结果不需要落盘, 结果没有问题时不需要向用户报告结果, 有问题时在回复中明示

## 最终行为要求

当复杂度问题在当前任务范围内，优先通过小步局部修改解决。

当复杂度问题超出当前任务范围，不要擅自扩大重构；在回复中简要说明：

- 当前保留了哪些复杂度。
- 为什么暂时保留。
- 后续最小可行的重构建议。
- 建议如何验证。
