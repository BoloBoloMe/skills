# 全局约束

## 回复约束
- 始终保持信息精炼
- 避免重复粘贴文档原文,必要时仅概括关键结论,并指向相关章节

## 环境与工具
- 必须通过 uv 来执行所有 python 命令

## Skill 使用流程

### 新需求

当用户提出新功能/新流程/新接口/新业务规则时:
1. 先使用 `/grill-with-docs` 澄清需求.
2. 不得在需求边界/状态流转/异常场景/幂等要求不清楚时直接写代码.
3. 需求澄清完成后,使用 `/to-prd` 整理 PRD.
4. PRD 确认后,使用 `/to-issues` 拆成垂直切片.
5. 实现时使用 `/tdd`,按一个测试/一个实现/一次验证的小循环推进.

### Bug 修复

当用户提出 bug/异常/线上问题/测试失败时:
1. 先使用 `/diagnose`.
2. 必须先复现问题或建立可信反馈循环.
3. 修复前给出可证伪假设和验证方式.
4. 确认根因后,再使用 `/tdd` 添加回归测试并修复.

### 陌生代码

当任务涉及不熟悉的模块/跨多个包的调用链/历史代码时:
1. 先使用 `/zoom-out`.
2. 先说明该模块在系统中的位置/上下游依赖和边界.
3. 不得只看局部代码就直接重构.

### 架构复盘

当用户要求优化架构/降低复杂度/改善模块边界时:
1. 使用 `/improve-codebase-architecture`.
2. 默认只输出诊断报告和改进建议.
3. 未经用户明确要求,不得直接执行大规模重构.

## 高风险任务

涉及以下内容时,默认需要人工确认,建议用户使用 `human-in-the-loop`:
- 支付状态流转
- 退款/扣款/补单/关单
- 风控判断
- 金额/币种/汇率/定价
- 幂等/重试/补偿
- 数据库结构变更
- 权限/认证/密钥/签名
- 生产配置/部署脚本/`CI/CD`

## 任务拆分原则
- 默认垂直切分:围绕一个最小业务结果,贯穿所需层次,使任务完成后可以运行/测试/验收.
- 禁止自己决定水平切分,只有在用户明确要求/基础设施改造/单层缺陷修复时,才可使用水平切分:按技术层拆分工作,如表结构/DAO/Service/Controller/配置/工具类分别成项.
