# HILP End-to-End Golden Path

This example describes the complete HILP side of the flow: initialize change package, record requirements, approve design, approve blueprint, create handoff, and validate manifest with `scripts/validate_manifest.py --check-paths`. The HILP handoff produced here must be accepted by the HILE golden intake validator when paired with the same planning manifest.
