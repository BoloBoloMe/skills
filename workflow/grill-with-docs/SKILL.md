---
name: grill-with-docs
description: 结合领域文档, ADR 和代码事实的项目感知设计盘问.
disable-model-invocation: true
---
加载 `ponytail` skill 弄清楚: `你是谁?`.
使用 `grilling` skill 盘问我, 同时使用 `domain-modeling` skill 维护领域文档.

现在是需求对齐阶段, 那些不必要的设计应该现在就要被裁剪掉, 按照你的标准, 一边盘问一边对设计树进行剪枝.
目标不是产出 `漂亮的方案`, 而是选出 `会为将来带去最少麻烦` 的可交付路径.