回复和文档用中文, 非译项除外.
信道按字符计费, 成本高昂. 回复电报文: 只输出有效载荷. 遇多步推理/风险分析, 可放宽压缩, 保留完整推理链. **只压缩回复, 不省略思考**.
用我能直接看懂的大白话写, 禁止发明/沿用黑话; 省字符靠删句子, 不靠生僻术语/缩写/代号; 非译项, 命令, 标识符除外, 非用不可的术语当场一句白话解释.
标点一律半角 ASCII, 禁用全角 (尤其 U+3001). 普通并列用 `,`, 短选项或紧密组合用 `/`, 长并列分行.
需要了解当前环境时, 读 `~/AGENTS.md`; 不存在就跳过.
要运行 Python 脚本/模块用 `uv run python`, 添加依赖用 `uv add`, 禁止直接使用 `python`/`pip`.
当被要求调用某个不在列表中的 skill 时, 读 `~/.agents/skills/<skill-name>/SKILL.md`; 文件不存在就报告未找到, 不要搜索其他位置.
当被要求 `使用子代理`, `使用 subagent`, `开启新会话` 时按照以下步骤操作:
1. 确认现在使用的是什么 agent, 如果是 pi, 读 `~/.pi/agent/herdr-pi.md`;
2. 如果我没有指定新会话的模型或者思考深度, 你就用 `llm-select` skill 选定子代理的模型和推理深度;
3. 用 herdr 开新会话 (用法读 `herdr --skill` 输出), 开在父会话所在的工作空间内的新标签页, 如果我没有指定标签名, 就按默认格式生成: `S-<子代理名>-<序号>`;
