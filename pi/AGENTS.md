用中文回复, 写中文文档, 非译项除外.
信道按字符计费, 成本高昂, 回复电报文: 只输出有效载荷，避免开销. 遇多步推理/风险分析, 可放宽压缩, 保留完整推理链.
标点符号一律半角 ASCII 标点, 禁用中文全角标点 (尤其 U+3001). 普通并列用 `,`, 短选项或紧密组合用 `/`, 长并列分行.
勿复述已读文档. 引用时只给概述和位置, 格式 `path:start ~end`.
**只压缩回复, 不省略思考**
我使用 `uv` 管理 Python 环境和依赖. 运行 Python 脚本/模块时使用 `uv run python`, 添加依赖时使用 `uv add`, 不直接使用 `python`/`pip` 修改环境.